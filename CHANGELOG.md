# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project intends to follow [Semantic Versioning](https://semver.org/) once public interfaces begin to stabilize.

## [Unreleased]

### Acceptance status

- Phase 1 step 108 is prepared after the real step-107 post-reboot verification passed on 2026-08-15 with 13 passes, zero failures, zero skips, and archive SHA-256 `71b23d0175203eb6dc3ea5d8a93353c5eb68bb2bf49d83f7ae92f9a141fa4a1c`. The accepted step-107 record SHA-256 is `28b1d780e35ad2c0fa7cda2deaf372475b6967145f74b572f5b2eb8307dfcce0`; it binds boot ID `626f1a3a-606a-4dd3-8ff2-64d78032cadf`, current boot epoch `1786811096`, the exact post-cleanup package/ELILO/boot/module baselines, continued rollback absence, and all three retained recovery archive hashes. Step 108 adds a read-only recovery-backup release review: script SHA-256 `f9d9f69ce33f0ac88ad514dd0955ff578a0ee552fbd0ce34b518fc0f48b5961e`, policy SHA-256 `107799edcc26434e331634375301b1a39520fa6b1d479d445f7a106646b7260e`, confirmation scope SHA-256 `7db595eea0276ee21bcc15be5c05d338b6d121a1b94d565d55943a6ed1ed1049`, exact future removal-target SHA-256 `fb4798006e393ce84856c7afed4484aaaa2990952dcc9928f0787c56fb57874d`, and focused-harness SHA-256 `80ed4996f69e9b4b25a29cac41ccdd4837ebeb48a272b7a4f8b3141fb3bfd470` with 40 passing checks. The review requires the same successful boot identity and requires the recovery directory to contain exactly three regular non-symlink archives (`boot.tar`, `modules.tar`, `pkgtools.tar`) with the accepted hashes. It performs no removal; a clean result may authorize only a later exact-file removal boundary.

- Phase 1 step 107 is prepared after the real step-106 post-apply reboot review passed on 2026-08-15 with 11 passes, zero failures, zero skips, and archive SHA-256 `8c8cbdf911a860ed2b0681a3888812e4d3af59869ac93b3ec337e996ea1fc244`. The accepted step-106 record SHA-256 is `0cf844a9fe0bd03420bca564b6d44cccfc6994b2d2a394643152f36fb9d4d9d4`; it preserves `reboot_ready=true`, `reboot_authorized=true`, `reboot_executed=false`, `recovery_backup_retained=true`, and the exact package/boot/module/recovery baselines. The separately authorized manual reboot then returned on kernel 5.15.209 with a versioned active `BOOT_IMAGE`; the executable ELILO configuration still contains only the active 5.15.209 kernel/initrd pair (the trailing rollback text is comment-only and is ignored semantically). Step 107 adds the read-only post-reboot verification: script SHA-256 `797d1876b07ff8459df937566c2aa40c629396ee161843566d9f063f8eeb870a`, policy SHA-256 `0f8d00197d61d2609aa8e7cf4a5601d4a4282d9ebb4d9de8103d0b897658b7d3`, confirmation scope SHA-256 `fcbb41a567a7faf64ad7db0a6c61074e63482aa18514efed9b35ea450d5852cf`, and focused-harness SHA-256 `3d5479163f8cd6f6b3c8f1319aef6d5af20da31d8955cc5f37d77619379624c2` with 32 passing checks. It proves the current `btime` is later than the accepted step-106 review epoch, validates the running versioned ELILO `BOOT_IMAGE`, rechecks the exact post-cleanup package snapshot, all four active boot-artifact hashes, exact ELILO bytes and semantics, the complete active module-object manifest, continued rollback absence, and all three retained recovery archives. Recovery-backup deletion remains explicitly unauthorized; a clean step 107 makes it eligible only for a later release review.

- Phase 1 step 106 is prepared after the real step-105 survivor-integrated ELILO cleanup committed successfully on 2026-08-15 with 12 passes, zero failures, zero skips, and archive SHA-256 `d3f68ec2a2947c75fddadbdae57246db7b535c926fc83952ef2d9960aa8ac0fa`. The accepted apply record SHA-256 is `429042886380d1ee119221a3cff0927258a328684b99fd039f80233a138d0b1c`: it records `apply_executed=true`, `apply_committed=true`, `recovery_attempted=false`, `recovery_restored=false`, `recovery_backup_retained=true`, `pause_safe=true`, and `reboot_required=true`. The committed state removes exactly the rollback 5.15.19 generic/huge/modules package records, retains the exact active 5.15.209 triple, preserves the complete active module-object manifest SHA-256 `4edc368213108a4d359d6ebe51ddd52e80669b6b78695fc7b64a5c89c4be2425`, removes all rollback module objects including the three separately authorized VirtualBox survivors, atomically leaves ELILO with only the active versioned pair, and removes only the two legacy EFI rollback artifacts. Step 106 adds a strictly non-mutating post-apply reboot review: review script SHA-256 `cb6bd676f6094866d5b2d85b524190e611c25823899d17769f12548a3204bd47`, policy SHA-256 `47997a3050ee35be70e608a93fa144d09fac316e6f928ad0968ba92c8ac6d96a`, confirmation scope SHA-256 `2de44097cbfdd6cfba2751f01470b4f48c2a3f740d7722669f2d4ca30cd0dfa7`, and focused-harness SHA-256 `f9def9109d3d56e1ca6aae04a45aa44c433338910425c5cd5102922583204bb8` with 35 passing checks. It revalidates the exact post-cleanup package snapshot SHA-256 `ab69ead76bd994fd5198fa0351b9ecc66a6b2af986965751750e54aaa69e054f`, active boot files, semantic single-entry ELILO configuration, zero rollback module objects, and the exact retained recovery archives before authorizing only a later manual reboot. Package/boot mutation, repository refresh, network access, recovery-backup removal, and reboot execution remain unavailable in the review itself.

- Phase 1 step 105 is prepared after the real step-104 third-attempt authorization review passed all 5 assertions on 2026-08-14 with archive SHA-256 `c9b208042303ed775ed4750b36e4665ed683db7e12a2f33e0544236a30cecdee`, `third_attempt_authorized=true`, `cleanup_authorized=true`, `apply_authorized=true`, `execution_authorized=false`, `apply_executed=false`, and `pause_safe=true`. The immutable accepted step-104 record SHA-256 is `1500829b085a0f10b2768671fec2d33d67d3f8fa355cc374f297dbf27bed3619`. Step 105 changes no destructive executor code: it activates only the exact survivor-integrated executor SHA-256 `7b42e2df3f99eaa7a92bbb2b91bcc97aa63d5cb0f755b935788409533ada937c` under production policy SHA-256 `98cfb6ead03debb184884da422d9050050f97060991c56cbf5806574e9ab919f` and confirmation scope SHA-256 `f9a1beb3973633e4f9af8b2571628f0b24d14338e915e11afc94ac6ef9849e37`. The gate remains bound to the canonical cleanup contract, exact step-94 and step-97 authorizations, exact step-102 survivor authorization, and exact step-104 third-attempt evidence. It permits unlink only of the three reviewed package-unowned rollback VirtualBox objects; recursive rollback-tree deletion, active-counterpart removal, repository refresh, network access, and automatic reboot remain forbidden. The existing recovery snapshot requirement and exact automatic restoration path remain mandatory. A successful transaction reports `apply_committed=true`, `pause_safe=true`, and `reboot_required=true`, then routes only to `elilo-oldkernel-cleanup-post-apply-reboot-review`. The focused production-boundary harness SHA-256 is `4631497044ffc578d33d3ea9df6fa1776a41d88cb59dd61167635e03a81119ab` and validates the immutable executor, all accepted-record identities, production-policy activation, safety denials, and the exact calculated scope before the real apply is run.

- Phase 1 step 104 is prepared from the accepted step-103 safe-pause checkpoint. The new boundary is strictly non-mutating and separately authorizes a third cleanup attempt only against the exact survivor-integrated executor revision accepted in step 103. It revalidates the running 5.15.209 kernel, exact recovered package/ELILO/module manifests, all three rollback VirtualBox survivors and their active 5.15.209 counterparts, and the three exact locally cached 5.15.209 kernel archives before emitting a fixed third-attempt authorization contract. The authorization contract sets `third_attempt_authorized=true`, `cleanup_authorized=true`, and `apply_authorized=true`, but deliberately keeps `execution_authorized=false` and `apply_executed=false`; repository refresh, network access, reboot execution, recursive rollback-tree removal, and active-counterpart removal remain denied. The prepared destructive apply policy remains unchanged and fail-closed with no accepted step-104 record, so this delivery cannot execute cleanup. Accepted step-103 record SHA-256 `72b60c339e572554aa94d8143fff606c624e312a812e86772efe3890ac60bdbd`, authorization script SHA-256 `8d3d51e9829cfc67e567c83c7684ebdba5e56edb44374c31a2546b9e2db10698`, third-attempt authorization contract SHA-256 `edbf2df2aaf764607cfed1f84abf848d9605027ac3de761cf0a9106e3a3a9033`, policy SHA-256 `ad3a4cf7e48501dbd8752939809446b8420b1be93b61db41777e13de1bf3e46c`, confirmation scope SHA-256 `3f4cd3c8816964bd3c48099805eb93a48c2d07c8a40039afcf1793ce797bbb1e`, and focused harness SHA-256 `26b6c6427be98c738408a480d51ad2d211c2ab06b250c43431da152d53b728e0` are fixed. The focused harness passes 18 checks with zero failures. A clean real result remains `pause_safe=true` and routes to `elilo-oldkernel-cleanup-authorized-apply-revision-2`, which will be prepared only after the step-104 evidence is accepted.

- Phase 1 step 103 is now accepted as a real-system safe-pause checkpoint after the non-mutating survivor-integrated executor review passed all 5 assertions on 2026-08-14 with archive SHA-256 `9e3c9d1c6aa462a7fb7fb09eb95ffebd9945faba94d8c3b74274dd61b400a1c0`. Before/after package, selected boot, active-module, and rollback-module manifests are byte-identical (`b295eaa1abde0f961d21227d51988cead0a7794a298a5a980b19fb312d7b897a`, `95bbc279c56b63d088bc25a54abda0dcdf0e70fa920fc32b22260bfafe9fdec5`, `a284a504efad1e6bcd200ab6dd453cafad557b575f4231a25bb12f06bcd23695`, and `49fb193b78b668eaf5dba1788c1e18861e6c77f083e9e50c5edc2a57965eed26`). The exact revised executor is accepted for later authorization review with `revision_ready=true` and `survivor_integration_ready=true`, but remains fail-closed: `third_attempt_authorized=false`, `cleanup_authorized=false`, `apply_authorized=false`, and `apply_executed=false`. The next stage is step 104, `elilo-oldkernel-cleanup-third-attempt-authorization-review`; it has not been prepared or executed in this checkpoint. This is a genuine pause-safe boundary: the VM may remain powered off without repeating steps 93-103 merely because Slackware repositories change, because the continuation is bound to accepted evidence and exact locally cached 5.15.209 package archives rather than a fresh metadata snapshot. Preserve `/var/lib/slack-update/elilo-cleanup-backups/` and the exact cached 5.15.209 kernel archives until the cleanup/reboot chain is formally closed. Accepted step-103 record SHA-256 `72b60c339e572554aa94d8143fff606c624e312a812e86772efe3890ac60bdbd`.

- Phase 1 step 103 is prepared after the real step-102 survivor deletion authorization passed all 5 assertions on 2026-08-14 with archive SHA-256 `405327481b0c7459aab7ddab5b1a2f325fffb6ac2312ed1b4a18afc36d32fe6a`. The accepted boundary authorizes only later unlink of the exact three package-unowned 5.15.19 VirtualBox survivors and still denies recursive rollback-tree removal, active-counterpart removal, execution, and a third cleanup attempt. Step 103 remains strictly non-mutating and reviews a revised transactional executor that integrates those exact three unlinks after rollback-package removal and active-package reinstallation validation, before ELILO mutation/final verification. Each unlink is path- and SHA-256-bound and rechecks the corresponding 5.15.209 module; the existing full rollback-module recovery snapshot covers the three files. The prepared executor is additionally fail-closed on a future accepted third-attempt authorization record that does not exist in this delivery, and its prepared apply policy retains `execution_authorized=false` and `third_attempt_authorized=false`. Review script SHA-256 `e675ab97d5650ef3ec92ba8fb4f5f49ef410ae4766201e87e083351b9396bf05`, revised executor SHA-256 `7b42e2df3f99eaa7a92bbb2b91bcc97aa63d5cb0f755b935788409533ada937c`, accepted step-102 record SHA-256 `59e1d20f96b52667555b616f6aca63db6fbf9286657d7c94ee7cad8f38ab5af6`, survivor-integrated revision contract SHA-256 `aa321fd44baa1db20cde121ff172c12efb80eb2629f80c9ebaf00bee3bc4fc5f`, prepared apply policy SHA-256 `4b3ce6ce5d33d24fec5b30b4f403b84796fffe1433fe4c8aa2a2cabec561ab38`, review policy SHA-256 `77f2ff3b54caeb6f0e33bf55765bcb795e427721cf23093d3801c04bbb85f2ce`, confirmation scope SHA-256 `5046db267744d0e759a36e57344eb2cc5e9dc41df28ca101530a1d10f540ab80`, focused review harness SHA-256 `fb24ad949ddca9389de72b3a0cea664b7e7667486d99c74887053e29cf4a490b`, and revised apply harness SHA-256 `221cf0c15f9d39623f2bed0bfcd0b455e4ef6bb28043f3f280c6e4d8b8676abb` are fixed. The focused review harness passes 17 checks and the revised transactional apply harness passes 20 checks, both with zero failures. The prepared inventory contains 66 suites and 3,859 checks; static validation covers 113 shell scripts and 128 JSON files.

- Phase 1 step 102 is prepared after the real step-101 survivor review passed all 5 assertions on 2026-08-14 with archive SHA-256 `90eb0d6a9c08acfc68993c8a6a967be17bdaa435aceef82b267483d5d05fe456`. The accepted review fixes exactly three package-unowned rollback VirtualBox objects (`vboxguest.ko`, `vboxsf.ko`, and `vboxvideo.ko`) under `/lib/modules/5.15.19/misc/`, together with their exact SHA-256 values, 5.15.19 vermagic, zero installed-package owners, and matching 5.15.209 active counterparts. Step 102 remains strictly non-mutating and authorizes only later unlink of those three reviewed rollback paths; recursive removal of `/lib/modules/5.15.19`, removal of active counterparts, a third cleanup attempt, cleanup apply, and execution remain denied. Script SHA-256 `f7ec25de2c6b2b9c8b30823f4601e4b2edcfab3c8777fbbfd966bb9712c59a9a`, accepted step-101 record SHA-256 `c946664eaa81065168983423f87793a296b3282b270b345485d838f53b8f1b39`, survivor deletion authorization contract SHA-256 `e1eb7c7ab124a23432e2f7101618a44765bff2702a97e225944ab8643b3db42b`, policy SHA-256 `29272f8666d9675db180389c8d4478ef15e9924ba670812dc3d81bb4529db17f`, confirmation scope SHA-256 `6d83c315fd64fbe0894f2d55aaadb84a106a6eae57ffb11508734ea0e9b0be84`, and focused-harness SHA-256 `7e46fe832926dd042c9d334dd486ef231c2ce4d6947db78a719a61f0c75d20c6` are fixed. The focused harness passes 19 checks with zero failures. The prepared inventory contains 65 suites and 3,837 checks; static validation covers 111 shell scripts and 125 JSON files.

- Phase 1 step 101 is prepared after the real step-100 instrumentation archive SHA-256 `ef8664635cd51be9a3b087ae2414afb8b35027d40462ec8b506b453f8c0b04c1` confirmed the final cleanup root cause: exactly three package-unowned rollback module objects survive `removepkg`, `/lib/modules/5.15.19/misc/vboxguest.ko`, `vboxsf.ko`, and `vboxvideo.ko`. The new step is strictly read-only. It revalidates the exact recovered package, boot, active-module, rollback-module, and retained private-recovery boundary; proves that no installed package record owns any of the three rollback paths; requires each object to be a root-owned, non-writable regular file whose direct `modinfo` name and vermagic match 5.15.19; and requires the exact active-kernel counterpart below `/lib/modules/5.15.209/misc/` with the same module name and a 5.15.209 vermagic. The resulting survivor-removal contract fingerprints both rollback and active counterparts and explicitly forbids recursive rollback-tree deletion, active-counterpart deletion, and third-attempt authorization. Cleanup/apply remain denied until a separate authorization review accepts this exact three-file scope. Script SHA-256 `232ef17f7fbae30f03b68111381ac423afd30c5a4f2f35a21c1d67294f16535b`, accepted step-100 record SHA-256 `7f9076d382113d9d5e05746ab98ee8b168b310aa8514d99cd497483f9d038503`, policy SHA-256 `edbed0b13eda260af34f4ba2b239dfbe7c64eec80deec09fac1e41f8ac5d592d`, confirmation scope SHA-256 `f5e65d581ca8be463773950f2cd643f7e7da352e9eb208711f4bab9f1a9444d5`, and focused-harness SHA-256 `9d50e51e3ef4e613f41160f0dfdf2bfe53d8f549da8d1b7add60b311ee5fa61f`. The focused harness passes 17 checks with zero failures. The prepared inventory contains 64 suites and 3,818 checks; static validation covers 109 shell scripts and 123 JSON files.

- Phase 1 step 100 is prepared after the accepted step-99 diagnostic archive SHA-256 `a967cec7cf1b81f1abcf8d8df5bc02973c8990a6103440b3e6dceecc795eb44d` returned `depmod_dry_run_rc=0`, leaving the second cleanup false negative unresolved but the VM recovered and `pause_safe=true`. The new step is strictly read-only and recognizes that the old aggregated failure covered both `capture_final` and `verify_final`. It revalidates the exact recovered package, boot, active-module, rollback-module, and private recovery state; decomposes every captured/final predicate with its evidence basis; and compares the live `/lib/modules/5.15.19` regular-file inventory with the union of the exact three rollback package `FILE LIST:` records. Package-unowned `.ko*` files are therefore identified before another destructive attempt as objects that would survive `removepkg` and violate the final rollback-module predicate. Third-attempt authorization remains false regardless of the result. Script SHA-256 `459691b4b51ba721270741b022c670a207e8eaaed2d7f70c933fc848ffa21247`, accepted step-99 record SHA-256 `60df5ef645b3a3cb4116abe1427ed9c24ce3f136fc412de1865de7311f77814f`, policy SHA-256 `6b4b1dc138eda8fd262e93af600b82f4248ca8650747c7564d105c8442e917f2`, confirmation scope SHA-256 `a6b1e736b8984fa4b119ae5ea7fdfec7e6e9e3f87c6e043395c0c2c765edfc41`, and harness SHA-256 `c7fda63a7eaeba684d4dfe4b4aaaa4575e41f00fd96510d32bbc3dc930ff318e` are fixed. The focused harness passes 17 checks with zero failures. The prepared inventory contains 63 suites and 3,801 checks; static validation covers 107 shell scripts and 121 JSON files.

- Phase 1 step 99 is prepared after the real step-98 revised ELILO cleanup attempt again reached the reviewed package/ELILO cleanup path but failed the aggregated final contract and then restored the exact pre-apply state. The failed archive SHA-256 is `e44fd3dbb0f0ba9ca2e2d131c36c6b8389b234855028b7581baf2994ece6f3bd`; `apply_executed=true`, `apply_committed=false`, `recovery_restored=true`, `pause_safe=true`, and `reboot_required=false`. Offline evidence comparison proves that the final package set was exactly the expected set with only the three 5.15.19 rollback records removed, the revised stable active-module manifest and every `.ko*` object were byte-identical, and the only six changed active-module files were the already reviewed generated depmod indexes. Because revision 1 still reported only one aggregated final-contract failure, step 99 adds a strictly read-only final-contract diagnostic instead of authorizing a third destructive retry. It revalidates the exact recovered package, boot, active-module, rollback-module, and retained recovery state, then runs only `/sbin/depmod -n 5.15.209`, captures its exit status/stdout/stderr, and proves the probe did not modify packages, boot state, or modules. A non-zero depmod result confirms the remaining root cause and routes to a depmod-validation revision; a zero result routes to instrumentation of the remaining final predicates. Third-attempt authorization remains false in all cases. Diagnostic script SHA-256 `6fac9f9e07202ea392bbad8efa7f9c094e4eb5d13ad5cef10a88734783722cf0`, failed-attempt diagnostic record SHA-256 `b59e003a016f8bfcae4351303f410ed7876c24d69a7c7249c9bd0c4fb3ec914c`, policy SHA-256 `9196efc205793890f11a26a782fb0eb8e65600f2ebed959f6129387170bb792f`, confirmation scope SHA-256 `31a091a8a82bba78e67b2dcf121d0e580932e700bca0b1c61007fefdd2420c2e`, and focused-harness SHA-256 `7f99af635e7cca38e1c28f7276415ae13f3d4162de3531747ac97c682c0db1c9` are fixed. The focused harness passes 10 checks with zero failures. The prepared inventory contains 62 suites and 3,784 checks; static validation covers 105 shell scripts and 119 JSON files.
- Phase 1 step 98 is prepared after the real step-97 revised-apply review passed all 4 assertions on 2026-08-11 with archive SHA-256 `4ed50105ad880742638c91426cdc3d9e9a8dcd04425f5fe74709e9ae708024e7`, `retry_authorized=true`, `cleanup_authorized=true`, `apply_authorized=true`, and `apply_executed=false`. Step 98 does not widen the cleanup contract: it activates the already reviewed revised executor for a second transactional attempt, still bound to the original step-94 cleanup contract SHA-256 `e5b587aacb911a05428706a09c3d7a85dc35a9802e46ccf8131cb3569dd6806f` and additionally to the accepted step-97 review record. Only the six reviewed top-level depmod indexes may regenerate; every `.ko*` object and every other active-module-tree file remain byte-identical, followed by read-only `depmod -n 5.15.209`. Package/ELILO actions, EFI deletion scope, persistent private recovery capture, automatic recovery, no-network/no-refresh policy, and post-success reboot review are unchanged. Revised executor SHA-256 `680bf2e4b18f3ff9a3939c4fa947a689347ed15c4f25df343241f62fe55a21de`, accepted step-97 record SHA-256 `7f927e0238ad95b5240a41430ae6e9ab76e825869b7929fe996721e51c14e1c4`, production policy SHA-256 `a9b868de8d4a642dff0e203e75c6b9432f682022f94d66e96a9a2825f6099b0b`, confirmation scope SHA-256 `423c19029954bf893443cced45f556cb121913889cdc94d9391ef4dcbb8b2585`, and production-boundary harness SHA-256 `823f9203f1865d6823ce33bdad99a25c8396825d5d256b596e30765b2ca26874` are fixed. The existing revised apply harness passes 15 checks and the new production-boundary harness passes 14 checks, both with zero failures. All 60 pre-existing reference suites are byte-identical to step 97; the prepared inventory contains 61 suites and 3,774 checks. Static validation covers 103 shell scripts and 117 JSON files.
- Phase 1 step 97 is prepared after the real step-96 recovery review passed all 4 assertions on 2026-08-11 with archive SHA-256 `2c1b622653231a3a499ce4aa4076414f7edcfa9d7fdb174b102d7144665742d4`. The VM had been rebooted before that review, and the review still proved the exact restored package, boot, active-module, rollback-module, and retained private recovery state, so the intervening reboot did not invalidate recovery. Step 97 is strictly non-mutating: it reviews and reauthorizes a revised cleanup executor that treats only `modules.alias{,.bin}`, `modules.dep{,.bin}`, and `modules.symbols{,.bin}` as regenerable depmod indexes, while every `.ko*` object and every other file in the active module tree remain byte-identical. A read-only `/sbin/depmod -n 5.15.209` validation is also required. The revised executor is deliberately not runnable in production until this review produces accepted evidence. Revision-review script SHA-256 `7b0728b59ea251d40586ca2bd22583efb7772ad1224802662f3c86fadf0f1ba9`, revised apply script SHA-256 `680bf2e4b18f3ff9a3939c4fa947a689347ed15c4f25df343241f62fe55a21de`, revision contract SHA-256 `5054c7126abf2b9d2694640318934d25fc41d60d5830802ac9f10af29fb15aea`, policy SHA-256 `85f5917969d5a65dd4ce852d7e1c6530656d6b8efa33cb0237dbdb169026e1f8`, and confirmation scope SHA-256 `8944c790b26b82d9b8a348417695f7e42730baca5216cf7935b2037bf948a368` are fixed. The revised apply harness passes 15 checks and the revision-review harness passes 12 checks, both with zero failures. The inventory contains 60 suites and 3,760 checks; static validation covers 102 shell scripts and 116 JSON files.
- Phase 1 step 96 is prepared after the real step-95 ELILO cleanup apply reached the reviewed cleanup state but failed its final byte-identity assertion because `upgradepkg --reinstall` legitimately regenerated six `depmod` index files for 5.15.209. The failed archive SHA-256 is `96a634ddfb4a131dec836a000d8e216b4716ace1924cecbb7f0490f48773131f`; the transaction then restored the exact pre-apply package, `/boot`, active-module, and rollback-module state and reported `recovery_restored=true`, `pause_safe=true`, and `reboot_required=false`. Evidence analysis proves that only `modules.alias{,.bin}`, `modules.dep{,.bin}`, and `modules.symbols{,.bin}` changed, with zero `.ko` object changes and the exact expected post-cleanup package set already reached before the false-negative assertion. Step 96 adds a strictly non-mutating recovery review that binds this diagnostic, verifies the retained private recovery archive, rechecks the live recovered state against the pre-apply manifests, and keeps cleanup/apply authorization false until a revised executor distinguishes immutable module payload from generated depmod indexes. Script SHA-256 `598278ce708afdb4bd6604474f0be8df22ad141c2a2bff7f9469c36fa8872a86`, policy SHA-256 `696fad23cab28a8fea30fd7cdd68edadf09331f5348dda046bdd444552084487`, failed diagnostic record SHA-256 `62fc603678bb5d5644a8d1f825eed9415fdba962b9e99cf75eba908f8ad08524`, confirmation scope SHA-256 `1606e8e9a25f3eb9a9da372bc7133f9e20fd8dde73f012d93f0c0de6e53fd5e2`, and focused-harness SHA-256 `b7268c33e7926fda90a96f51577664c66f78b0f4a4f76708ea56e6d44f91dc49` are fixed. The focused harness passes 17 checks with zero failures. The inventory now contains 59 suites and 3,767 checks; static validation covers 100 shell scripts and 113 JSON files.
- Phase 1 step 95 is prepared after the real Slackware 15.0 ELILO cleanup authorization review passed all 4 assertions on 2026-08-11 with archive SHA-256 `9ed0b6f4c989e4ea5d1742fc47d2ae5c31979e64fc3dffcc1aa7e5ed15934553`, `cleanup_authorized=true`, `apply_authorized=true`, and `apply_executed=false`. The accepted authorization fixes canonical cleanup contract SHA-256 `e5b587aacb911a05428706a09c3d7a85dc35a9802e46ccf8131cb3569dd6806f`. Step 95 adds the real authorized transaction executor: before the first package mutation it revalidates the exact step-94 boundary and creates a private recovery snapshot of `/boot`, both active/rollback module trees, and `/var/lib/pkgtools` under `/var/lib/slack-update/elilo-cleanup-backups/`. It removes only the three exact 5.15.19 rollback package records, reinstalls the exact authenticated 5.15.209 generic/huge/modules archives with the native package tools, removes exactly the reviewed `oldkernel` ELILO stanza, verifies the active versioned ELILO boot chain, and deletes only the two authorized legacy EFI rollback artifacts. `/boot/initrd.gz` is deliberately retained because it is outside the authorized deletion contract. Any failure after mutation begins must attempt exact recovery of package, boot, and module state; `pause_safe=true` is allowed only after a committed coherent cleanup or proven byte-equivalent recovery. A successful transaction retains the private recovery snapshot until a later post-apply reboot verification and sets `reboot_required=true`; the executor itself never refreshes repositories, uses the network, or reboots. Script SHA-256 `33178bd9cd99db62629c24e930e2853b837a23d06ba5f7b84967bf15f98f5789`, policy SHA-256 `8572410d7a5a94202f882b093439b96298b60b58b6164b6c1ba727b7b8727b61`, accepted step-94 record SHA-256 `4289573e994c900fcec0e4a039dac75141bebabfc4ee9289adb08d5b4ce0d40d`, confirmation scope SHA-256 `006e228aee659ae65c18a7f4a541245dd9ed8bfa32a1de8d820e30603db56938`, and focused-harness SHA-256 `63abbddf3967e1ceeec6eb5745943edf6fdc0c2320bd231326aac570b8aabf14` are fixed. The focused harness passes 34 checks with zero failures. All 57 pre-existing suites remain unchanged from step 94, so the prepared inventory contains 58 suites and 3,750 checks; static validation covers 98 shell scripts and 111 JSON files.
- Phase 1 step 94 is prepared after the real Slackware 15.0 ELILO cleanup source-and-plan preflight passed all 7 assertions on 2026-08-11 with archive SHA-256 `1f2cb93ff141c5474cd106304d03f1edd8084cefc90bad81cc3701db06846201`, `source_ready=true`, `plan_ready=true`, and `cleanup_ready=true`. The accepted step-93 record fixes cleanup plan SHA-256 `2082e56223033181c32b3561591da59f1a630734641cccb07a68aa0a71efa1bb`, the exact cached 5.15.209 generic/huge/modules archive SHA-256 values, the three exact 5.15.19 rollback package records, the active/rollback ELILO artifacts, the fourteen ordered actions, and the preservation invariants. Step 94 adds a strictly non-mutating cleanup-authorization review: it revalidates the running 5.15.209 boundary, package-name snapshot, ELILO/boot snapshot, populated 5.15.209 and 5.15.19 module trees, and all three cached active archives before issuing authorization only for canonical apply contract SHA-256 `e5b587aacb911a05428706a09c3d7a85dc35a9802e46ccf8131cb3569dd6806f`. A clean review records `cleanup_authorized=true`, `apply_authorized=true`, and `apply_executed=false`; `removepkg`, `upgradepkg`, ELILO mutation, rollback-file deletion, repository refresh, and reboot remain unavailable until a separate apply stage. Script SHA-256 `dff8905103fa864bd5e5bc78c5e220baed3cddba0adc2416ff95e28ec7bead9b`, policy SHA-256 `4ab6b74b2b3bea4bd82c59e260e5e8e8bffef0394892ddbcfb8871d30efd3568`, accepted step-93 record SHA-256 `2154bb568b48ed313837858dc2bc3e8406e25c7a6b354282c8ba9d348f2cef09`, and confirmation scope SHA-256 `bde7ac7470138574f0c2616d4b68a62311c57c32f6b4596277c60f0c9e760328` are fixed. The focused harness passes 15 checks with zero failures. All 56 pre-existing suites remain unchanged from step 93, so the prepared inventory contains 57 suites and 3,716 checks; static validation covers 96 shell scripts and 109 JSON files.
- Phase 1 step 93 is prepared after the mature Slackware 15.0 ELILO oldkernel retention rerun passed all 24 assertions on 2026-08-11 with archive SHA-256 `dacf3bf5ecbe1464b9aacd42457f47dcf91d8e79eed61a29bed40173e89c81af`, `cleanup_eligible=true`, and `cleanup_authorized=false`. The accepted mature record preserves active kernel `5.15.209`, rollback `5.15.19`, exact six boot-kernel package records, byte-identical `/boot` and EFI artifacts, the elapsed seven-day window, and the later successful boot. Step 93 adds a non-mutating ELILO cleanup source-and-plan preflight. It refuses repository refresh or downloads, authenticates the locally cached Slackpkg `CHECKSUMS.md5` metadata with the reviewed Slackware primary fingerprint, requires exactly one cached archive for each active `kernel-generic`, `kernel-huge`, and `kernel-modules` 5.15.209 package, verifies each archive against the authenticated MD5 metadata and structural safety, then feeds their SHA-256 identities into the canonical cleanup planner. A successful plan must be mature and blocked only by `cleanup-authorization-not-granted`; the existing dry-run engine must render exactly fourteen inert actions with `commands_executed=[]`, `mutations_performed=[]`, and `apply_authorized=false`. Package names and ELILO artifacts are compared before/after, and real cleanup remains denied. Script SHA-256 `85260001640e7930764c9f36bb884c8a57e63020fe74d12069a4b14effd718ec`, policy SHA-256 `e1be506cd85a956bd7f9a1250ebf8c2b060c97a8fe83d4e34dcd8580fa4e5c8c`, accepted mature-retention record SHA-256 `5ffd8e41dd466e259e50b27164df4d34082b979b30e3eda39afc67ee7c36e29f`, confirmation scope SHA-256 `8dec059faaf5af26d59311e0efe80e2a1c7ffd4b4ffe2f8e362ad0228514b3c8`, and focused-harness SHA-256 `58a43c9eabdfbcb3b0f741868fc4f8a79c264eba0e6cb304a76c715d2a32f35d` are fixed. The focused harness passes 27 checks with zero failures. All 55 pre-existing suites remain unchanged from step 92, so the prepared inventory contains 56 suites and 3,701 checks; static validation covers 94 shell scripts and 107 JSON files.
- The real Slackware-current optional rollback demonstration is closed and accepted: the step-92 archive SHA-256 is `4fb16a55e48441f6e74cda471ff776d82f179eb396f7365d43d5f86499179334`, normal return to `6.18.42` is verified, tested rollback `6.18.40` is retained, `pause_safe=true`, and `current_work_remaining=false`. The copied sidecar used an absolute `/var/tmp` path, but the uploaded archive digest was independently recomputed and matched; this does not reopen the closed current branch.
- Phase 1 step 92 is prepared after the real step-91 rollback boot verification passed all 5 assertions, proved a real 6.18.40 boot, authorized an unchanged normal return, and produced accepted archive SHA-256 `c4a8e207ad3af7ef118c2371f3cec0694c1e5fca50599d4b5a2e89daa2b77d7f`. The host has now rebooted normally and `uname -r` reports `6.18.42`. Step 92 adds a strictly non-mutating final return-verification closure: it binds the accepted step-91 record, requires the running kernel and osrelease to be 6.18.42, requires `BOOT_IMAGE=/boot/vmlinuz-generic` with the reviewed root UUID, revalidates the exact active kernel/initrd and generic links, proves the tested 6.18.40 kernel/initrd and 5,490-object module tree remain complete, verifies the active GRUB entry remains first/default, requires one unique rollback entry and clear grubenv state, and compares package plus boot-sensitive snapshots before and after. A clean result records `normal_return_verified=true`, `rollback_retained=true`, `rollback_boot_demonstration_closed=true`, `pause_safe=true`, and `current_work_remaining=false`, then routes the project to the pending Slackware 15.0 ELILO preflight repeat without refreshing Slackware-current metadata. Script SHA-256 `a3663e1a254a7bcc24b427681ec581e96b325f40314fb713eccfd9040731f2eb`, policy SHA-256 `06fcc1c72b9405db7edd64f1590ad7a9f1c9edeff07ab3c08a100f0ee16a5a10`, accepted step-91 record SHA-256 `ba33afa568a550ef16659861d25dc41769d2bec6ae76ad8d47658a12f35e64a0`, confirmation scope SHA-256 `083e44cb173729870b9f825da8363b4576ea501767c126a5384f4438863de8de`, and focused-harness SHA-256 `c2b797f5fffb075298e3c9a7305d05c3839236556df5c63a49d78c6ce5c166e3` are fixed. The focused harness passes 33 checks with zero failures. All 54 pre-existing suites remain unchanged from step 91; the step-92 inventory therefore contains 55 suites and 3,674 checks, with static validation covering 92 shell scripts and 104 JSON files.
- Phase 1 step 91 is prepared from the accepted real step-90 boot-authorization archive SHA-256 `f78f4a249c1fa44b1e6d171f1f283cd6679dd90623a34686fcf225d1cc1681fa` after the host was manually rebooted into the reviewed rollback entry and `uname -r` reported `6.18.40`. Step 91 adds a strictly non-mutating rollback-boot verification and return review: it requires the rollback kernel and `/proc/sys/kernel/osrelease` to be 6.18.40, verifies the reviewed root source/UUID and command-line root, rejects a conflicting `BOOT_IMAGE`, revalidates the exact 6.18.40 kernel/initrd and 5,490-object module manifest, checks module `vermagic`, proves the active generic links and first GRUB entry still target 6.18.42, requires one unique rollback entry and clear grubenv state, and compares package plus boot-sensitive snapshots before and after. A clean result both proves the real rollback boot and authorizes only a normal reboot with no manual rollback selection so the unchanged default returns to 6.18.42; the review itself does not refresh repositories, mutate packages or GRUB, edit grubenv, or reboot. Script SHA-256 `b5b129a6c973fed4ad13f26e9bd6e06dfc14e9cdec780fabcfe05801d157ae3b`, policy SHA-256 `efb415c3e4cd4615a1f0c7b72b7dac564fe69015ec5dea79c8a12153072c2e54`, accepted step-90 record SHA-256 `d5a503ca478be5a12589eae61e8061b0616671c04b230e85904adae23967b2c4`, confirmation scope SHA-256 `247fefc960f7dc333ffe64328baaaff04e03fbe42ca17f144145ed862472684a`, and focused-harness SHA-256 `e7eff8126240b17b8917f9e4306b2e199ad93ee8b6e8b7e9c7c2b62340178a41` are fixed. The focused harness passes 27 checks with zero failures. All 53 pre-existing suites and executable test inputs remain byte-identical to step 90; the step-91 inventory therefore contains 54 suites and 3,641 checks, with static validation covering 90 shell scripts and 102 JSON files.
- Phase 1 step 90 is prepared from the accepted real step-89 reconstruction archive SHA-256 `dc19821f783ad334a49073eaf7596fdbf48471bd8e20fb87753266b9159f475d`. Step 90 adds a strictly non-mutating rollback boot-authorization review: it binds the accepted reconstruction record, exact host identity and review code; proves the package database still matches the accepted post-reconstruction snapshot; verifies active 6.18.42 kernel/initrd hashes and generic links; verifies the versioned 6.18.40 kernel, gzip-valid initrd, exact 5,490-object installed module manifest, depmod metadata, rollback GRUB fragment, syntax-valid active GRUB configuration, unique rollback entry `slackware-rollback-6.18.40`, active generic entry first, and absence of pending or saved rollback selections in grubenv. A clean result authorizes only one manual selection of `Slackware GNU/Linux (rollback 6.18.40)` on the next reboot; it does not mutate grubenv, GRUB, packages, repository metadata, or reboot the host. Script SHA-256 `3e02371aa4d50bf61ab96185948a21b9fde97c5402bd3411287be43675db29b2`, policy SHA-256 `a4d5009bc809bf4ac346c81534d3a44c4fea0ae2eaa336624da9637135a00376`, accepted step-89 record SHA-256 `83f1db00313c8249a8c08e3d3b3764df4093385c96f5669b29b8c4d984d5f36b`, confirmation scope SHA-256 `5a3ea1d21c691d16259f2e7409e5c684391f1a0ead1a4eafe105e7291a7f96e7`, and focused-harness SHA-256 `3e735c8d5ff76a680c4554bcaa66ce7b1111583abce02fd1ac81cfa55ede0680` are fixed. The focused harness passes 15 checks with zero failures. All 52 pre-existing test suites and executable test inputs are byte-identical to the accepted step-89 delivery, whose complete regression passed 3,599 checks; the step-90 inventory therefore contains 53 suites and 3,614 checks, with static validation covering 88 shell scripts and 100 JSON files.
- Phase 1 step 89 is prepared after the real step-88 authorization review passed all 8 assertions, emitted `apply_authorized=true`, retained `apply_executed=false`, and produced accepted archive SHA-256 `8da16204e339d8c1a4e5756ba938a6f22ecb4528a67e32a726954c935c612fb5`. The immutable accepted record binds the exact local signed 6.18.40 source, canonical apply contract `1a2a59d73a72cb609bc1622e5882adddfc1a6f117d6602e0ba3abe238badd923`, reviewed kernel and 5,504-file module manifest, versioned mkinitrd vector, two-microcode rollback GRUB entry, owner-only backup root, staging root, and no-mutation authorization result. Step 89 adds `test-current-rollback-reconstruction-authorized-apply.sh`, a single transactional apply-and-verification boundary that reruns step 88 before mutation, preserves the module placeholder, active GRUB configuration, and `/boot/initrd-tree`, stages and verifies the exact payload, restores kernel/modules, runs depmod and the reviewed mkinitrd vector, validates a temporary GRUB configuration, keeps 6.18.42 running and first/default, atomically replaces GRUB, and proves the package database and generic links remain unchanged. Successful completion reports `rollback_complete_on_disk=true`, `pause_safe=true`, and routes only to the optional boot test; failures after mutation automatically restore and compare the captured baseline, and may report pause safety only when restoration is proven. Script SHA-256 `d98fa9546cc2be38a27436a279f740bd4d7427163287cc774bd77496acde0644`, policy SHA-256 `960599ca8763b8e67d65f770b4957eb25b36e5666f455836d045e3488acbb839`, accepted authorization-record SHA-256 `5f65bc36c7f00a4a7baa601ecd0f4732452cff4132ddfb8dde2b8fc5532978a4`, confirmation scope SHA-256 `df59fe7d67ce54859da0db6c07f055d67ae6ce212498d3a28926432ec0610af9`, and focused-harness SHA-256 `8313e892a2659181400ec1cadc6f11aa6a87bcb2bea8d9580f8e59aca09ed6db` are fixed. The focused harness passes 70 checks with zero failures. The complete regression result is `52 suites and 3,599 checks with zero failures`; static validation covers 86 shell scripts and 98 JSON files.
- Phase 1 step 88 is prepared after the real step-87 revision-6 run passed all 61 assertions with zero failures or skips, left the host unchanged, and produced accepted archive SHA-256 `ce45977bbcacb237163d821a43d5a79f5246bfca54bc3fb4ca6edfc30243fbfb`. The immutable accepted record `slackware-current-rollback-source-and-plan-preflight-20260806-accepted.json` binds the exact signed 6.18.40 package, kernel SHA-256, complete 5,504-file module manifest, conservative space budget, reviewed `mkinitrd` vector, ordered Intel-plus-AMD microcode vector, explicit rollback GRUB fragment, active default `0`, twelve ordered apply actions, and all no-mutation results. Step 88 adds `test-current-rollback-reconstruction-authorized-apply-review.sh`: it revalidates the accepted record and explicit review scope, checks the retained source hashes, reruns step 87 revision 6 into nested evidence, semantically audits every fresh plan artifact, captures package and rollback-sensitive state before and after, and emits `apply-authorization.json` only when all boundaries remain unchanged. The authorization is valid only for the canonical apply contract whose SHA-256 is fixed by the policy; step 88 itself keeps repository refresh, package installation, package-database mutation, `depmod`, `mkinitrd`, GRUB mutation, and reboot forbidden, with `apply_executed=false`. Review script SHA-256 `db031cf1185da410efdac5229a34e823ce90d32d29b0c258e08e89488b5c0725`, policy SHA-256 `9c315dd0d7da4196d1ab2c69353ff7a42abe66742a7a73ba8c236e365a501e4b`, accepted-record SHA-256 `95e24090154ac0d4a1d714100ac19ddbb6d2740961f675439a571334f318c597`, canonical apply-contract SHA-256 `1a2a59d73a72cb609bc1622e5882adddfc1a6f117d6602e0ba3abe238badd923`, and confirmation scope SHA-256 `03751103875a56b092e92bd4f915016320e5663000598a7c0fe3c5023aad2b50` are fixed. The focused harness passes 46 checks with zero failures, including wrong scope, changed source, incomplete nested evidence, and retained-active-initrd rejection. The complete regression result is `51 suites and 3,529 checks with zero failures`; static validation covers 84 shell scripts and 96 JSON files.
- Phase 1 step 87 revision 6 is prepared after the real revision-5 run passed 58 assertions, failed one GRUB-vector assertion, skipped two dependent stages, and left the host unchanged; its archive SHA-256 is `ff6d4a338b2da9ce4547128f6d0472f781dd47300f2b82658cae18cf94b6c8da`. Revision 5 correctly rejected the active entry because the host contains two reviewed early-microcode images (`intel-ucode.img` followed by `amd-ucode.img`) before the active generic initrd, while its policy allowed at most one. Revision 6 binds that diagnostic, requires zero to two unique reviewed microcode images in source order followed by exactly one reviewed active initrd, preserves the microcode sequence, and replaces only the active initrd with `/boot/initrd-6.18.40.img`. It rejects duplicates, unknown arguments, foreign initrds, active-initrd retention, and microcode after the initrd; failed projection no longer emits a secondary `chmod` error and writes `grub-projection.log` plus `grub-source-entry.json`. All package, `depmod`, `mkinitrd`, GRUB, metadata-refresh, and reboot actions remain forbidden. Script SHA-256 `d306e3c1d9d5a2cc2298d6367601d39e6573569211db422fdbfda5cb4e5aba8b`, policy SHA-256 `92bcaf0e2038d4979e3c6f8dc054171053af5e5d555dba223aa7e670ebabbcaa`, revision-5 diagnostic-record SHA-256 `22d8108ff3dc2a87d741c9a899508a27e5fd4f0bce8f06cbb5049dd39f18f117`, and confirmation scope SHA-256 `92cc5d316f66ea2205751b83e88a3f749c6ec4f7d7a8a534106ebc5cc7d38f6e` are fixed. The focused harness passes 125 checks with zero failures. The complete regression result is `50 suites and 3,483 checks with zero failures`; static validation covers 82 shell scripts and 94 JSON files.
- Phase 1 step 87 revision 5 is prepared after semantic review rejected the otherwise successful revision-4 plan. The real revision-4 run passed 61 assertions with zero failures and zero skips, authenticated and inspected the exact signed 6.18.40 package, proved sufficient space, and left the host unchanged; its archive SHA-256 is `11122a514f1f1c75cfcb52e5d4f310ed7719235c23f4389b6e16e3d13f513134`. However, its projected GRUB entry replaced only the first initrd argument, yielding `initrd /boot/initrd-6.18.40.img /boot/amd-ucode.img /boot/initrd-generic.img`: this reordered microcode and retained the active 6.18.42 initrd, so the reported `apply_ready=true` is explicitly superseded and may not authorize reconstruction. Revision 5 binds that rejected plan diagnostic, requires the active entry to contain exactly one reviewed active initrd plus at most one AMD or Intel microcode image, projects microcode first followed by only `/boot/initrd-6.18.40.img`, and rejects foreign initrds, unknown arguments, duplicate microcode, or any retained active initrd. It emits `projected-grub-entry.json` and binds the corrected vector into `reconstruction-plan.json`; all actions remain non-mutating and unauthorized. Script SHA-256 `0a923befda11399ddcf18b886036627e236fcf7d26b8686dc21c93296c331fb8`, policy SHA-256 `e845d4cecfbcfee2d9e005444fb3ddb208b6d3a55f88bc144af004ba3e3ac864`, rejected-plan record SHA-256 `5ae40ea0f89c4999e6edd6b277b13c7c46c352783418af8d3ecddd7bb59d5a86`, and confirmation scope SHA-256 `72f58b575c93c478fb5562733a526fa4c283b76222a4c5090b7e5f4da343b7aa` are fixed. The focused harness passes 115 checks with zero failures. The complete regression result is `50 suites and 3,473 checks with zero failures`; static validation covers 82 shell scripts and 93 JSON files.
- Phase 1 step 87 revision 4 is prepared after the non-mutating revision-3 run passed 58 checks, failed one space-budget assertion, and skipped two dependent stages. Its immutable archive SHA-256 is `45b5b35fd51ef962d5865185679eeb28f3a2435ddddbca70500608706f4396ca`. Exact source acquisition, SHA-256 validation, detached-signature verification, complete package inspection, live 6.18.42 boundary, package database, rollback placeholder, GenInitrd, and GRUB checks all passed. The sole defect was the mutually exclusive `df -P` and `--output=avail` combination on Slackware. Revision 4 binds this fourth diagnostic, uses `df -B1 --output=avail`, retains any command diagnostics in `space-budget-df.log`, and leaves every reconstruction action unauthorized. The revision-4 script SHA-256 is `2a0e98ed08e138385ca2983d1f7047c1ef5a54613b50d0305fd5f9414ad47099`, policy SHA-256 `0dbb529f5f478ac2865f0dd00e7451637dd14d08952382eb9200b6750221d83f`, diagnostic-record SHA-256 `1ab88f07299fbbe8366365cbc6e480819e261f2b30bc337a70d9d41ac1043145`, and confirmation scope SHA-256 `055be31d9d09f7fb8f4e1c1b4f7142e8d56a211a642ee346e727e99b75128531`. Package operations, `depmod`, `mkinitrd`, GRUB mutation, metadata refresh, and reboot remain forbidden. The focused revision-4 harness passes 97 checks with zero failures. The complete repository regression passes 50 suites and 3,455 checks with zero failures; static validation covers 82 shell scripts and 92 JSON files.
- Phase 1 step 87 revision 3 is prepared after the non-mutating revision-2 run passed 57 checks, failed one package-inspection assertion, and skipped three dependent stages. Its immutable archive SHA-256 is `bc28dd82557236f0938f71c4255eee6ea2f477f2f8676f32209af6fae3ce420e`; exact package acquisition, SHA-256 validation, detached-signature verification, live 6.18.42 boundary, package database, rollback placeholder, GenInitrd, and GRUB checks all passed. The sole defect was that the inspector rejected the normal root directory member `.` in the official Slackware `.txz`, then attempted to parse outputs that had not been published. Revision 3 binds this third failed diagnostic, accepts zero or one normalized archive-root member only when it is a safe root-owned directory, excludes it from payload paths, writes package summary and module manifest atomically, and records deterministic inspection failures without secondary missing-file tracebacks. Absolute paths, traversal, duplicate normalized paths, unsafe links, special members, unsafe modes, non-root ownership, missing kernel payload, and missing module objects still fail closed. The revision-3 script SHA-256 is `409a4558a4bb92712ad192158962267159a3dd037c3f0159056ad969f7e63291`, policy SHA-256 `f72406bdcda511532c206209792f4c467e64cd3c8c985970d3602acd86701845`, and confirmation scope SHA-256 `130d459ec76ba27eb1e8468bf48b4c2ae4b097250e03d3226a85424270ab6e0f`. Package operations, `depmod`, `mkinitrd`, GRUB mutation, metadata refresh, and reboot remain forbidden. The focused revision-3 harness passes 94 checks with zero failures. The complete repository regression passes 50 suites and 3,452 checks with zero failures; static validation covers 82 shell scripts and 91 JSON files.
- Phase 1 step 87 revision 2 is prepared after the non-mutating revision-1 run passed 41 checks, failed one undifferentiated pre-staged source-acquisition gate, and skipped five dependent stages. That immutable diagnostic archive SHA-256 is `5dc24b1863a818cd0500fd08ea569995e627411a66181ccebcbb74698bbac35e`; its script SHA-256 was `8dc3eceb45c6c531aaf8e3f74e907cf4eb2b45a3aa113f43876b57405def47bd`, and all package, kernel, rollback placeholder, initrd, GenInitrd, and GRUB state remained unchanged. Revision 2 binds both failed step-87 archives and adds exact per-file acquisition diagnostics for basename, `lstat`, symbolic-link rejection, regular-file type, `O_NOFOLLOW` opening, positive size, and SHA-256. It records these results in `source-acquisition-checks.tsv` and `source-acquisition.json`, verifies the detached signature through an isolated temporary `gpgv` keyring without `gpg-agent`, and rechecks both source hashes after signature validation. Pre-staged inputs are retained in place, while the evidence publisher prints exact archive-plus-sidecar copy commands and no longer suggests copying a file onto itself. The revision-2 script SHA-256 is `516397c136ab9dd75d90eba7a5e1f969ef36c30e1725e05a2e3fbf3060b09c93`, policy SHA-256 `33a6ce326bfd189e5be8cd3dbffffaa5c97c69f6dd9ac2787598c43b188306a0`, and confirmation scope SHA-256 `571f30dacef5001b21bf6890ea13ad56dddb48c769dea456f01b03033dd8b27b`. Package operations, `depmod`, `mkinitrd`, GRUB mutation, metadata refresh, and reboot remain forbidden. The focused revision-2 harness passes 82 checks with zero failures. The complete repository regression passes 50 suites and 3,440 checks with zero failures; static validation covers 82 shell scripts and 90 JSON files.
- Phase 1 step 87 revision 1 is prepared from the exact ZIP that was executed on the host, ZIP SHA-256 `c888299b410347df8676f8ab7558fe1011349c8ac3a69ce1607ccecacde57c66`. The initial script SHA-256 `37756428b0fbb9e106ce1853414f8032d803fdc6bb9ec9fef642ed82bd4c8a74` produced a rejected, non-mutating diagnostic archive SHA-256 `d2a5398c789f14cfee07d53a55e4ca7da8aab85dd0387b51f437120401f9ba14`: it incorrectly required `/lib/modules/6.18.40` to contain no entries, nested `GNUPGHOME` below an evidence path too long for the `gpg-agent` Unix socket, reported dependent stages as additional failures, and projected `rmdir` for a nonempty metadata placeholder. Revision 1 binds that failed evidence, the corrected step-86 diagnostic, exact signed package SHA-256 `90dcee03829a3755b74b52a679fadea2619d96a38e12c461ee6e6866c9c598cc`, detached-signature SHA-256 `1222c391801227dc389dcd46de91d1915e61df12f436614ebd0a24b1697fdd39`, reviewed Slackware primary fingerprint `EC5649DA401E22ABFA6736EF6A4463C040102233`, revised script SHA-256 `8dc3eceb45c6c531aaf8e3f74e907cf4eb2b45a3aa113f43876b57405def47bd`, and revision scope SHA-256 `5e215b8c53becb65b216fadb2e38bbeb57f8fc590856e405728db76fc60efb95`. It accepts only the exact `depmod-metadata-only-placeholder` entry set, reports every live-boundary check separately, uses a short `/tmp/slack-update-gpg.XXXXXX` home, verifies a signing subkey only when GnuPG chains it to the reviewed primary key, and records dependent prerequisites as skips rather than false failures. The projected apply boundary moves the complete metadata placeholder into retained backup storage before restoring the signed kernel and module tree; it never assumes `rmdir` can succeed. Package tools, `depmod`, `mkinitrd`, GRUB mutation, metadata refresh, and reboot remain forbidden. The focused revision harness passes 68 checks with zero failures and uses a fixed public-key/signed-package fixture so regression runs never generate or retain a private test key. The complete revision inventory passes 50 suites and 3,426 checks with zero failures; static validation covers 82 shell scripts and 89 JSON files.
- Phase 1 step 86 remains a rejected diagnostic boundary, not an accepted reconstruction inventory. Reinspection of its evidence and the failed step-87 archive corrected the rollback state again: `/lib/modules/6.18.40` contains no kernel module objects, but it has one empty `misc` directory and eleven root-owned depmod metadata files, so the accurate classification is `depmod-metadata-only-placeholder`, not `empty-directory-placeholder`. Archive SHA-256 `cd2769c18e93b17596028b33b00a1d6e14bb81336172935bb18b0bef3568ed56` remains the immutable real inventory evidence. The exact historical package was absent from `/var/cache/packages`, while free space, the 2,040-record package database, active 6.18.42 kernel/initrd/modules and generic links, GenInitrd controls, and GRUB state remained unchanged. The corrected diagnostic is `slackware-current-rollback-reconstruction-inventory-20260806-corrected-diagnostic.json`; complete reconstruction must back up and replace the metadata placeholder, restore both `/boot/vmlinuz-6.18.40` and the full `/lib/modules/6.18.40` tree from one exact signed package, and only then create the rollback initrd. This correction does not reopen the mandatory update chain closed by step 85.

- Phase 1 step 85 is complete and accepted. The corrected real Slackware-current post-reboot verification passed all 14 assertions with zero failures and archive SHA-256 `5d6fe97ddd81d1c99d0dd807127d6e98b8479d8e719d6c6ffb346fe167c915eb`. It proves the live x86_64 kernel and osrelease are 6.18.42, `BOOT_IMAGE=/boot/vmlinuz-generic`, the reviewed root UUID and boot ID are valid, the exact 2,040-record package transaction remains installed, the target kernel/initrd/modules and generic links are correct, GenInitrd controls are unchanged, and GRUB still selects the reviewed generic pair without `next_entry`. Package and sensitive before/after snapshots are byte-identical, the host was not mutated, the record classified rollback as `degraded-modules-only` based on the then-available path check, and the accepted record SHA-256 is `2735cb3f3a30270432984661dfcdcd5a3e3787190c34867a01651ffcfaaa678a`. The checkpoint records `pause_safe=true`, `reboot_verified=true`, `update_closed=true`, `mandatory_work_remaining=false`, and `next_stage=optional-rollback-reconstruction-review`. The mandatory update chain is closed and later Slackware-current publications do not require any part of it to be repeated. Step 85 adds no executable code or suite; the inventory remains 48 suites and 3,311 checks with zero failures, with static validation covering 78 shell scripts and 84 JSON files.
- Phase 1 step 84 is complete. The corrected read-only Slackware-current post-reboot verifier binds the accepted step-82 authorization and archive SHA-256 `e41138a31225a795c65ddf76fa75fde19072f5892c9719af32d06b564027937b` to corrected script SHA-256 `7cc42682d0d464f5b923be823141bae1c458edaf28a852a73983f123cef36deb` and verification scope `06f6a7a43bd8f51ddb72452964fd7b55cab6fafb69c0fb4e56c0369ce9cb8416`. The GRUB resolver now evaluates the active top-level conditional branch, so the canonical `next_entry` branch plus literal `default=0` fallback is accepted only when `grubenv` contains no nonempty `next_entry`. All package, kernel, initrd, module, GenInitrd, GRUB, rollback, and before/after invariants remain unchanged. A 14-pass result records `pause_safe=true`, `reboot_verified=true`, `update_closed=true`, and `next_stage=optional-rollback-reconstruction-review`; no metadata refresh, package operation, boot-artifact mutation, GRUB edit, or reboot is permitted. The focused harness passes 83 checks. The complete prepared inventory contains 48 suites and 3,311 checks with zero failures; static validation covers 78 shell scripts and 83 JSON files.
- Phase 1 step 83 is complete as a rejected diagnostic boundary. The real post-reboot run proved the live x86_64 kernel and `/proc/sys/kernel/osrelease` are 6.18.42, `BOOT_IMAGE=/boot/vmlinuz-generic`, the reviewed root UUID and nonzero boot ID are valid, the exact 2,040-record package transaction remains installed, the target kernel/initrd/modules and GenInitrd controls are unchanged, and rollback is honestly `degraded-modules-only`. It passed 12 assertions and failed two closure assertions solely because the verifier required exactly one top-level `set default` assignment and rejected the standard GRUB structure containing inactive `set default="${next_entry}"` plus active fallback `set default="0"`. The observed `grub.cfg` SHA-256 remained exactly `5fdff76d42ddec26b0c212668c4981a9ea2853a98b3260f33850c91ccf8ac247`, the on-disk `grubenv` SHA-256 remained `f64122858064885ef0733e42c6a3d2d3fd642671f714db0d974b880c0f087430`, package and sensitive snapshots were unchanged, and the host was not mutated. Archive SHA-256 `bc5d8bd480f5c74cf6ab9a7ac5729b7f90925dcafb69f24daa1d228209ce8263` was verified in `/home/promano`. Manual recovery and another reboot are not required; only the corrected local verification must be rerun.
- Phase 1 step 81 is complete and accepted. The real Slackware-current reboot review passed all 13 assertions with zero failures, preserved the exact 2,040-record package database and all reboot-sensitive state, and resolved GRUB selector `0` to `Slackware-15.0+, with Linux generic` without a `next_entry` override. Archive SHA-256 `e41138a31225a795c65ddf76fa75fde19072f5892c9719af32d06b564027937b` was copied to `/home/promano` and verified. The accepted boundary records `pause_safe=true`, `reboot_ready=true`, `reboot_authorized=true`, `reboot_executed=false`, and `next_stage=manual-reboot-to-reviewed-target`; it authorizes exactly one manual reboot toward 6.18.42 without refreshing Slackpkg metadata or repeating the completed chain.
- Phase 1 step 64 is complete. The real Slackware-current candidate refresh passed all 8 outer assertions plus the nested 6-pass normal-update preflight, preserved package and boot state, and accepted digest `27eb06d282b4279f90f422235363c36897ff45f334607c00287384b848a8d926` for 137 candidates. The previous 69 candidates remain byte-for-byte present, 68 upgrade-only userspace candidates were added, no candidates were removed, no configured critical candidates appeared, and the exact `6.18.42` generic/headers/source transaction is unchanged. Archive SHA-256 `f457b123f4eac52b7e9721a6103fd6983819128e18f75c5967e5592ce8246fd3` and nested archive SHA-256 `96ea916726896864f53a7e2c7a1f7a748040668dd76d438d060475b269b9647b` were verified in `/home/promano`; apply remains denied.
- Phase 1 step 65 is complete. The real Slackware-current userspace candidate review passed all 11 outer assertions plus the nested 6-pass normal-update preflight, preserved package and boot state, and accepted the exact 137-candidate digest `27eb06d282b4279f90f422235363c36897ff45f334607c00287384b848a8d926`. The 68 additions remain exactly 61 Plasma 6.7.4 packages, six supporting userspace packages, and the single `breeze-grub` theme package; all remain `upgrade-all`, no configured critical candidates appeared, and the exact `6.18.42` kernel transaction is unchanged. Outer archive SHA-256 `7d22806577aa4432436d8760e86fdeec5af25a420fe05b28a5150c2c97617037` and nested archive SHA-256 `11812569b828f31874c01e1c7bcacef069c36fe8391c938ae545495d5f9cd1fc` were verified in `/home/promano`. The review permits only an explicit kernel-evidence rebind; payload inspection, userspace apply review, readiness, and authorization remain false.
- Phase 1 step 66 is complete. The real Slackware-current kernel-evidence rebind passed all 12 outer assertions plus the nested six-pass normal-update preflight, preserved package, kernel, versioned-initrd, GenInitrd, DKMS, and GRUB state, and rebound all seven accepted `6.18.42` evidence records from candidate digest `918ded076efb3ff0131b296ceae8854765dd5e92cc433542c498276f9aeba3f9` to `27eb06d282b4279f90f422235363c36897ff45f334607c00287384b848a8d926`. Outer archive SHA-256 `38a79511d6c17686b3a2b3e8c349c2c199264849ca1fd0222135d0ab1f00b482` and nested archive SHA-256 `24e8e2146029e9f4aadcf189acee5ababec7ecb89395074517363489aa83556e` were verified in `/home/promano`. The rebind changes only the candidate binding; payload inspection, userspace apply review, readiness, and authorization remain false.
- Phase 1 step 67 is complete as a rejected diagnostic boundary. The real Slackware-current run preserved the exact 137-candidate digest, resolved all 68 reviewed package archives, and passed 13 assertions before failing two payload-boundary assertions on the first explicit `boot` directory member in `breeze-grub-6.7.4-x86_64-1.txz`. The policy had stale `usr/share/grub/themes/breeze/` path data instead of Slackware's `boot/grub/themes/breeze/` layout. Outer archive SHA-256 `b61a17d2fb296d8493bab82dfcbb2bdf4bbebcb0b5168f6d61895674c31b45e8` and nested archive SHA-256 `bae2e427e5c789d3f51141cb8281e0493754f1721b6ba0ecff3af52ca219fe0e` were verified in `/home/promano`. Package, kernel, initrd, GenInitrd, DKMS, and GRUB state remained unchanged; payload completion, readiness, and authorization remain false.
- Phase 1 step 68 is complete. The corrected real Slackware-current payload review passed all 16 assertions, preserved the exact 137-candidate digest `27eb06d282b4279f90f422235363c36897ff45f334607c00287384b848a8d926`, and inspected all 68 reviewed package archives without installation. It accepted 37 syntax-valid but unexecuted `doinst.sh` files, 46 configuration paths, 44 service paths, 722 ELF files, and 24 exact Breeze GRUB theme paths. Outer archive SHA-256 `763313828522239da29e3fee5fc2582c14aabb8aa7b5c64fe9e0392ebc2c71ac` and nested archive SHA-256 `818c40e9cf5e776607d110881d876eaf75937292fc4cfd1814a243b0979ece90` were verified in `/home/promano`. Package, kernel, initrd, GenInitrd, DKMS, and GRUB state remained unchanged; maintainer-script review, userspace apply review, readiness, and authorization remain false.
- Phase 1 step 69 is complete. The real Slackware-current maintainer-script review passed all 15 assertions, reran the accepted 68-package payload inspection, and statically classified all 37 exact `doinst.sh` files without executing them. It accepted 1,159 remove operations paired one-for-one with 1,159 symlink creations, two `.new` promotions, five cache refreshes, and the sole reviewed `TERM` signal to `kscreenlocker_greet`. Outer archive SHA-256 `62f87bde4c4b1d49ff6c02476ce4a61af6420fa0798cd13ee9b6f83536762062`, nested payload archive SHA-256 `9bb11f5feec1c4b9be9b38d384717da30c05265b83fa852e5e71bf6d28ea4634`, and nested normal-update archive SHA-256 `98a82f0af75db3019e851ddf8026b5c300839b91abbc19c6db770f359857bcb5` were verified in `/home/promano`. Package and boot-sensitive state remained unchanged; configuration/service review, userspace apply review, readiness, and authorization remain false.
- Phase 1 step 70 is complete. The real Slackware-current configuration/service review passed all 15 assertions, reran the complete non-installing maintainer and payload chain, and verified the exact 46 configuration plus 44 service paths from 19 package archives. It statically validated 21 configuration files, 26 systemd user services, and one user preset, with zero system units or `rc.d` scripts. Outer archive SHA-256 `b2e6379879297e81bc2da8fa6aa58ccdaf6c904fe3018691ba6d78f463077140`, nested maintainer archive SHA-256 `5aed2e4895cde3611c41d8ba13f83add40c5e3c24db55429cc9a17a07b6b5d2f`, nested payload archive SHA-256 `b0493bec292d964158b7c7c46596ce269db8b05aaf446d47571097609487739f`, and nested normal-update archive SHA-256 `7290a56c9e45ea27226accfa19b175bfbb414c91bf7f9b7951ed3eb375948a96` were verified in `/home/promano`. Package and runtime-sensitive state remained unchanged; ELF runtime review, userspace apply review, readiness, and authorization remain false.
- Phase 1 step 71 is complete. The real Slackware-current ELF/runtime review passed all 15 assertions, reran the full non-installing configuration/service chain, and inspected all 722 ELF objects from 61 exact package archives without executing payload or dynamic-loader tracing. It classified 566 dynamic and 156 executable objects, accepted 158 reviewed interpreters, indexed 601 transaction SONAMEs, and resolved all 14,639 dependency edges through 13,970 unaffected host-cache resolutions plus 669 reviewed transaction providers, with zero unresolved or unsafe objects. Outer archive SHA-256 `b4803f5ef86d5383bc191f7087795b8ecb757bc1aad8bb3a42f4c021c4a1a562` and nested configuration/service archive SHA-256 `fa6867beb3a59500c5c52d09a9dddcc0ed93662ded96d68d2d69d94f3e345d63` were verified in `/home/promano`. Package and runtime-sensitive state remained unchanged; userspace apply review, readiness, and authorization remain false.
- Phase 1 step 72 is complete as a rejected diagnostic boundary. The real Slackware-current userspace application review preserved the exact 137-package transaction and passed 13 assertions while the embedded ELF/runtime review itself passed all 15 checks with 722 objects, 14,639 resolved dependency edges, zero unresolved edges, and zero unsafe objects. The sole outer failure was verification of an expected adjacent `nested/elf-review.tar.gz` and sidecar that the child does not create when given an explicit `--output-dir`; the child instead populated the requested directory and separately published its canonical archive under its own evidence root. Outer archive SHA-256 `bec1280dde4d51aa8db99698cd4d92df2270e1f318774ea215a8e02269249168`, child-reported canonical ELF archive SHA-256 `83a6c3eaf5956413eac97eec23439965486e292b709acbf63727416f1f59fabb`, and nested normal-update archive SHA-256 `7faaec43c20170508c3d04effeb90e556eeb19b8aec7da5091bc6a3e1742d538` were recorded; the outer archive and sidecar were verified in `/home/promano`. Package and boot-sensitive state remained unchanged; userspace apply completion, readiness, and authorization remain false.
- Phase 1 step 73 is complete. The corrected real Slackware-current userspace application review passed all 15 assertions, preserved the exact 137-package transaction, and verified both nested evidence boundaries. It accepted the exact 69-package baseline plus 68 reviewed userspace additions, one `install-new`, 136 `upgrade-all`, the unchanged three-package `6.18.42` kernel transaction, zero critical candidates, the reviewed Slackpkg and deferred-postinstall contract, GenInitrd restoration, exclusive GRUB ownership, 12 transaction steps, and five recovery boundaries. Outer archive SHA-256 `bf627728dcf330b1d22b885466c9f538bc28743ace83af30958ae4555c2a5522`, nested ELF archive SHA-256 `32653f141fb895166c8b422b4e80e0e7dd53c157d809f5085b10388e20706b36`, and nested normal-update archive SHA-256 `5ce4fdc0c4870402890683114b1d2ac30e1a3e3a1a671a7b969a0f022655147b` were verified. Package and boot-sensitive state remained unchanged; readiness and authorization remain false.
- Phase 1 step 74 is complete. The real Slackware-current readiness run passed all 10 assertions, refreshed metadata, revalidated the exact 137-candidate digest `27eb06d282b4279f90f422235363c36897ff45f334607c00287384b848a8d926`, verified the 69 exact cached archives, and retained the accepted versioned GenInitrd, no-op DKMS, and exclusive GRUB ownership boundary. Outer archive SHA-256 `d49af0c2f95f6ceaaa6f4073a5567b914f53ee9605724f78fea4a30afc463783` and nested normal-update archive SHA-256 `be24bafd8d1340ee781a6994835e64298413076e5a1ff9821a8cee3de6a54631` were copied and verified in `/home/promano`. It correctly reported `readiness=apply-ready`, `apply_ready=true`, `pause_safe=false`, and `apply_authorized=false`; no package or boot mutation occurred.
- Phase 1 step 75 is complete as a rejected diagnostic boundary. The real authorized-apply wrapper passed the accepted readiness/policy binding and captured package plus boot-sensitive state, then failed closed before package execution because `hostname` returned `pcold-slack.pcold-slack.org` while the reviewed command confirmed the short name `pcold-slack`. The same hostname comparison was duplicated inside boot-state validation, producing a second misleading boot-drift failure even though `sensitive.before.txt` and `sensitive.after.txt` are identical. No child apply ran, the package database remained unchanged, kernel `6.18.40` remained active, and `pause_safe=false`. Archive SHA-256 `909b7866a06c1f5d0cd53193af5b8e9c6f4d6ac4145f12491bc0265c9ca849b2` was verified in `/home/promano`.
- Phase 1 step 76 is complete as a rejected diagnostic boundary. The corrected host identity checks passed and the nested normal-update workflow refreshed and revalidated the exact 137-candidate digest, but the reference engine aborted during runtime boot probing with `BOOT_CMDLINE_FILE: unbound variable` before any package action. The outer wrapper recorded 8 passes and 7 failures, the child exited with code 1, the installed package database and boot-sensitive state remained byte-for-byte unchanged, kernel `6.18.40` and all rollback artifacts remain available, and `pause_safe=false`. Outer archive SHA-256 `6733b71f8ca3cd33c01b06268f8e2b0e6a2ece21593e53f1e5bf96ca04cefa0d` and nested apply archive SHA-256 `075d9af8fec50aaeb8312d88f95a4b0ee827b827d8cded173920b6dda01ed54e` were verified.
- Phase 1 step 77 is complete as a rejected diagnostic boundary. The corrected command-line input reached the unchanged reference engine, but the direct-generic probe then expanded an unset `GENERIC_KERNEL_LINK` before package execution. The child exited with code 1, the generated JSON was empty, and the parent produced secondary target-artifact failures even though both package inventories and both sensitive-state snapshots were identical. Outer archive SHA-256 `1aa2a16df16604603eabf91aa1100a52f3b4143689444a70c4465b9bb666317e` and nested apply SHA-256 `144f1826f68ea979d9ca70ad7f0e758df97057ffdac5e85b6cf55a1ea44760cd` were verified; kernel `6.18.40` and all rollback artifacts remain intact, no package transaction ran, and `pause_safe=false`.
- Phase 1 step 78 is complete as a rejected post-package diagnostic boundary. The real authorized run refreshed and revalidated the exact 137 candidates, completed both Slackpkg package phases, changed the installed database from 2,039 to the exact reviewed 2,040-record snapshot, installed the 6.18.42 kernel, headers, and source records, generated `/boot/initrd-6.18.42.img`, and retargeted the generic kernel and initrd links. The accepted engine then returned stable code 3 because its pre-transaction `auto` classification saw the generic link move away from the still-running 6.18.40 kernel and blocked its own GRUB regeneration. Outer archive SHA-256 `176e23caa8d20166fe0a0011f7f953308cd64d0374a2f9d45926e4260596b37c` and nested apply SHA-256 `c98174b96bb15513637c3c5c10c331d424b7f82109eb7732a832a8ce076a40d9` were verified. The active GRUB file remained byte-identical, the running session remained on 6.18.40, the old module tree remained present, but the old versioned kernel image and initrd were removed; `pause_safe=false` pending exact non-mutating recovery verification.
- Phase 1 step 79 is complete and accepted as the first genuine Slackware-current safe-pause boundary. The real non-mutating verification passed all 13 assertions, matched the exact 2,040-record installed transaction snapshot, verified the installed 6.18.42 kernel, module tree, versioned initrd, both generic links, restored GenInitrd controls, and the unchanged syntax-valid GRUB configuration that pairs the generic kernel and initrd in one menuentry. Package and boot-sensitive state remained byte-for-byte unchanged during verification. The rollback state is recorded honestly as degraded: the running 6.18.40 session and module tree remain, but its disk kernel and initrd are absent. Archive SHA-256 `b2e3ee1d4bcdc243afbde0160d7d7f50e985e365f9580551fea6def4d6ae1f96` was copied to `/home/promano` and verified. The accepted result records `pause_safe=true`, `reboot_ready=true`, `reboot_authorized=false`, and `next_stage=current-kernel-post-apply-reboot-review`; later Slackware-current publications no longer invalidate the completed transaction.
- Phase 1 step 80 records the accepted safe-pause checkpoint in `slackware-current-post-package-boot-recovery-20260805-accepted.json`. No host mutation or new execution boundary is added. Work may stop here safely. The next session must begin from the accepted local 6.18.42 boot pair and design a separate reboot-review boundary without refreshing repositories, changing packages, or treating the degraded 6.18.40 rollback as complete.
- Phase 1 step 81 is prepared as the separate non-mutating reboot-review and authorization boundary. It binds the accepted step-79 archive and immutable checkpoint, exact 2,040-record package state, 6.18.42 kernel/initrd/modules and generic links, GenInitrd controls, current `BOOT_IMAGE=/boot/vmlinuz-generic`, unchanged GRUB digest and syntax, empty one-time `next_entry`, and the effective GRUB default entry. It explicitly requires acknowledgement of the degraded 6.18.40 rollback, never refreshes repositories, never changes packages or boot files, and never executes the reboot. The focused harness passes 68 checks, including literal, saved-entry, generated-ID, and submenu selectors plus fail-closed drift cases. The complete prepared inventory contains 47 suites and 3,228 checks with zero failures; static validation covers 76 shell scripts and 80 JSON files. The real host run is pending and must be reviewed before the manual reboot.
- Phase 1 step 27 is complete: the no-updates scenario is accepted on Slackware 15.0 and Slackware-current.
- Phase 1 step 28 is in progress. The non-destructive preflight and ten-package Slackware-current transaction remain reviewed, with target-specific revalidation of the hardened deferred `.new` policy still pending on Slackware-current. Slackware 15.0 has passed the non-boot-kernel normal-update branch: an accepted preflight authorized an exact 196-package set after temporarily deferring `kernel-generic`, `kernel-huge`, and `kernel-modules`; the real apply installed 12 new packages, changed 184 upgrade candidates, returned stable code `4` for critical userspace updates, preserved boot state, and reported 45 pending `.new` files through the hardened policy.
- Phase 1 step 29 is complete. Slackware 15.0 accepted the UEFI/ELILO branch, installed kernel `5.15.209` alongside `5.15.19`, generated and verified a versioned initrd, atomically activated the versioned ELILO entry, rebooted successfully into `5.15.209`, and retained the `oldkernel` rollback entry.
- Phase 1 step 30 is complete. The corrected non-destructive ELILO oldkernel retention baseline was accepted on 2026-08-01, and the mature rerun on 2026-08-11 again passed all 24 assertions with archive SHA-256 `dacf3bf5ecbe1464b9aacd42457f47dcf91d8e79eed61a29bed40173e89c81af`. Exactly three active plus three rollback kernel package records remain installed, package and boot state stayed unchanged, the seven-day window is met, and the required later boot into `5.15.209` is observed. Cleanup is now eligible but remains explicitly unauthorized pending the separate source/plan and authorization boundaries.
- Phase 1 step 31 is complete as a design-only boundary. A boot-loader-neutral cleanup planner now validates exact active and rollback package triples, active archive coverage, module trees, package-database safety, and backend-specific boot state without executing package, boot-loader, or file-removal commands. ELILO plans stage atomic removal of `oldkernel`; GRUB plans require same-directory generation, syntax validation, active-entry verification, and atomic replacement. Every plan remains `apply_permitted=false` and `cleanup_authorized=false`.
- Phase 1 step 32 is complete as a dry-run-only boundary. The cleanup simulator validates the plan identity, exact action order, backend transaction metadata, and an optional plan-bound authorization whose scope is limited to simulation. Ineligible, unauthenticated, and single-kernel plans fail closed or produce zero-step blocked/no-op results. Authorized synthetic plans render all proposed command vectors and recovery paths while recording `commands_executed=[]`, `mutations_performed=[]`, `apply_authorized=false`, and `real-apply-stage-unavailable`.
- Phase 1 step 33 is complete. The corrected 2026-08-03 Slackware-current normal-update preflight passed all six assertions, preserved package and GRUB state, and accepted an exact set of 57 candidates: one `install-new`, 56 upgrades, two configured kernel candidates, and no configured critical candidates. Candidate-set SHA-256 `d9199fcf6c5cd8c59b87b1bde9a955df2c55d0ac84f6dab37ed8e4c1830dcaf1` and archive SHA-256 `33a0d6eb20dfc777c4c5f8a0172f8344aab03a20ffd130d0fe95753ffce57cbc` are classification evidence only; apply remains unauthorized.
- Phase 1 step 34 is complete as a diagnostic boundary. The first Slackware-current boot-layout preflight proved the monolithic `kernel-generic` model and unchanged package/boot state, but correctly remained unauthorized after three assumptions failed against the real host: `/etc/mkinitrd.conf` and `/boot/initrd.gz` were absent, while `/boot/vmlinuz-generic` resolved to `/boot/vmlinuz-6.18.40` rather than `vmlinuz-generic-6.18.40`.
- Phase 1 step 35 is complete. The corrected Slackware-current boot-layout preflight passed all 20 assertions on `pcold-slack`, accepted the monolithic `kernel-generic` plus `direct-generic-no-initrd` GRUB layout, proved package ownership of `/boot/vmlinuz-6.18.40` and `/lib/modules/6.18.40`, confirmed the target `6.18.41` image and modules in repository metadata, and preserved package plus boot state. Archive SHA-256 `ed7462e70496cf38a52c211f3d5945438e5f1bad5b8d8eaa7b90079540381967` was verified after copying to `/home/promano`; discovery remains `apply_ready=false` and `apply_authorized=false`.
- Phase 1 step 36 is complete as a code-only boot-policy boundary. The reference engine now classifies a safe direct generic GRUB layout, suppresses initrd regeneration only for that exact layout, validates the post-update package-owned `vmlinuz-VERSION` symlink target and module tree before GRUB generation, requires the generated GRUB configuration to reference the validated kernel, and reports a failed transition as boot-unsafe. No package update or real-system mutation was executed in this step; apply readiness still requires a later transaction preflight against the exact downloaded kernel package.
- Phase 1 step 37 is complete as an exact-package evidence boundary. The real `pcold-slack` preflight passed all 12 assertions, verified package SHA-256 `b588e9e74258baaf2d5e05a1731981cb679f5665d50a3a91d9f02219c4a8024a`, inventoried 6,588 safe members including 5,490 target-module paths, confirmed the target kernel image and no embedded initrd, preserved packages plus boot state, and produced archive SHA-256 `d4f455dafb6783dc96e8cf45c45d00ef4e54d1e9b5dc2ae8e05b8db166b50888`. Manual evidence review also identified the package script's guarded `usr/sbin/geninitrd` invocation, so package integrity is accepted but apply remains blocked pending host-policy inspection.

- Phase 1 step 38 is complete as an accepted host-policy boundary. The real `pcold-slack` preflight passed all 10 assertions, preserved package, boot, and policy state, and confirmed that `kernel-generic-6.18.41` would invoke `geninitrd` with `AUTOGENERATE_INITRD=true`, effective generator `mkinitrd_command_generator.sh`, automatic GRUB update enabled, and transition `direct-to-generated-initrd`. Archive SHA-256 `3b807d2d00fce2b9986308f5cd252d97b483d4b8f1a397fad7d6f047b20421fd` was verified after copying to `/home/promano`. Two executable pre-install DKMS hooks require a separate content and state review, so apply remains blocked.
- Phase 1 step 39 is complete as an accepted no-op DKMS-hook boundary. The real `pcold-slack` preflight passed all 10 assertions, preserved package, boot, GenInitrd-hook, and DKMS state, and produced archive SHA-256 `95eec7f57d4ff9d3f254830428d5382a155f890b9e57553b71fbd4f661e30ebf`. Manual review confirmed DKMS 3.4.1 with zero status rows, no `/var/lib/dkms` state, no running `updates/dkms` tree, and no target module tree. The reviewed `dkms-bcachefs` and `dkms-nvidia` hooks therefore take their explicit no-registered-module paths and do not invoke `dkms install`; apply remains unauthorized.
- Phase 1 step 40 is complete as an accepted command-output-only GenInitrd boundary. The real `pcold-slack` preflight passed all 11 assertions, preserved package, boot, policy, DKMS, and generator state, and produced archive SHA-256 `391c56d5aa3d5e8cfa5eba2a259e0c78964d5825d10ce1cabd0124d7ef435814`. It accepted one inert current-kernel `mkinitrd` vector, projected that vector to `6.18.41` and `/boot/initrd-6.18.41.img` with 18 reviewed modules, and executed neither vector. Apply remains unauthorized.
- Phase 1 step 41 is complete as an accepted GenInitrd/GRUB ownership boundary. The real `pcold-slack` preflight passed all 11 assertions, preserved package, boot, active policy, and GRUB state, and produced archive SHA-256 `246a54dd81c1db6ce2e7d04cb5d6e4739249e4a2f0483edcb9c7a5f1e0e93ad3`. It proved that the active `AUTO_UPDATE_GRUB=true` assignment overrides an environment-only value, accepted a one-line evidence-local `false` policy, and selected a temporary atomic policy override with twelve ordered stages and five recovery boundaries. Apply remained denied.
- Phase 1 step 42 is complete as a code-only ownership implementation boundary. The reference engine now stages a same-directory root-owned backup and one-line `AUTO_UPDATE_GRUB=false` policy, rechecks the active fingerprint before atomic replacement, restores the original policy immediately after package operations, and repeats restoration from the signal/exit cleanup path. Unsafe ownership, writable modes, symlinks, duplicate or unexpected assignments, concurrent active-policy changes, or backup drift fail closed. A failed restoration retains the backup and marks the Slackware operation failed; structured output exposes requirement, application, restoration, residual-active state, and diagnostics. No VM or real package operation was used in this step.
- Phase 1 step 43 is complete as a code-only generated-initrd validation boundary. For an accepted `direct-generic-no-initrd` transaction whose active GenInitrd policy has `AUTOGENERATE_INITRD=true`, the engine now requires the post-package `kernel-generic` record, package-owned target kernel and module tree, a root-owned non-empty `/boot/initrd-VERSION.img`, the exact `initrd-generic.img` symlink, and continued absence of legacy `initrd.gz` plus `mkinitrd.conf`. GRUB validation now requires both the reviewed target kernel and the generated initrd before atomic replacement. Missing, stale, writable, ambiguous, or partial states fail closed. The fixture is explicitly synthetic, executes no commands, performs no mutations, and keeps apply unauthorized.
- Phase 1 step 44 is complete as an accepted fresh Slackware-current candidate-chain boundary. The real `pcold-slack` run passed all 8 outer assertions plus the nested 6-pass normal-update preflight, preserved the installed package database and active boot state, and accepted 69 candidates: one `install-new`, 68 upgrades, the complete `kernel-generic`/`kernel-headers`/`kernel-source` target trio for `6.18.42`, and no configured critical candidates. Candidate-set SHA-256 `918ded076efb3ff0131b296ceae8854765dd5e92cc433542c498276f9aeba3f9` and archive SHA-256 `f9be7efe4a46c72f070f63c4278600e6c75547fe05912f038a9d421c8049c7f7` invalidate every prior candidate-bound `6.18.41` record; apply remains unauthorized.
- Phase 1 step 45 is complete as a diagnostic boundary. The first real chain-restart run preserved package and boot state and produced verifiable outer archive SHA-256 `ea7b0d7fa6ff5f9020f41ae06f5bea5c91a79d579dddf1bc25d258ca484605d1`, but the nested boot preflight failed one assertion because the local Slackpkg file inventory did not expose `boot/vmlinuz-6.18.42` before the exact package was downloaded. The accepted package list still contained the complete `kernel-generic`/`kernel-headers`/`kernel-source` target trio, so the failure is diagnostic only and apply remains unauthorized.
- Phase 1 step 46 is complete as the accepted corrected restart boundary. The real `pcold-slack` rerun passed all 20 nested boot assertions and all 6 outer assertions, preserved the installed package database and active boot state, and accepted `target_image_metadata_state=deferred-to-exact-package-preflight` for target `6.18.42`. The nested archive SHA-256 is `be201626afc4b4aa2f9b59d7c10429e083094989b8c172530348aad95ab4fc32`; the accepted outer archive SHA-256 is `77618b808093f3e5349f5a6e076a110b56876a7ed08878d89c71a78fc594de51`. Apply remains unauthorized.
- Phase 1 step 47 is complete as the accepted restarted exact-package boundary. The real `pcold-slack` run passed all 12 assertions, preserved the package database and active boot state, and accepted `kernel-generic-6.18.42-x86_64-1.txz` with package SHA-256 `e9e7a1c5c71c945ee99595868aa8fee8a644b56601ece0c3e5696d643fe84878`, 6,588 safe members, 5,490 target-module paths, the exact target kernel image, no embedded initrd, and the reviewed conditional `geninitrd` hook. Archive SHA-256 `44c18026052a7d7b0d5e385258389f8bc73beefe9ac35c2a2777707df17c4f57` was verified after copying to `/home/promano`; apply remains unauthorized.
- Phase 1 step 48 was accepted against the then-assumed direct-boot chain, but is now historical only. The real run passed all 10 assertions and preserved state, yet its `direct-to-generated-initrd` model depended on the incomplete `direct-generic-no-initrd` classification later revoked by steps 53–55. Archive SHA-256 `873c7779dcef6f16d72d809704ca732809e6d5db5b1668f6a4942662b97c54ca` must not authorize apply.
- Phase 1 step 49 is complete as the accepted restarted DKMS-hook boundary for `6.18.42`. The real `pcold-slack` run passed all 10 assertions, preserved package, boot, GenInitrd-hook, and DKMS state, and produced archive SHA-256 `c943b3c25703fc395cfba6708a9c6122b033f82876795053f39a6d8e61ff5074`. DKMS 3.4.1 again reported zero status rows and both reviewed hooks remained explicit no-ops; apply remains unauthorized.
- Phase 1 step 50 is complete as a diagnostic boundary. The real command-output preflight safely projected the 18-module `mkinitrd` vector to `6.18.42` and `/boot/initrd-6.18.42.img`, preserved package and boot-sensitive state, and produced verified archive SHA-256 `8f3f32b17d735241caf7e22f2aa32b56d2d42590a46bb920979ed51e0ab6c3f6`, but one assertion failed because the cache check still compared the target package with the stale embedded SHA-256 accepted for `6.18.41`. The evidence is diagnostic only and apply remains denied.
- Phase 1 step 51 is complete as the accepted corrected GenInitrd command boundary for `6.18.42`. The real `pcold-slack` rerun passed all 11 assertions, preserved package, boot, GenInitrd, DKMS, and generator state, verified the exact cached package SHA-256 `e9e7a1c5c71c945ee99595868aa8fee8a644b56601ece0c3e5696d643fe84878`, and accepted the 18-module projection to `/boot/initrd-6.18.42.img`. Archive SHA-256 `be239e39372ad807be01199051730dbaf2602b962ceebea68b864e5951c78682` was verified after copying to `/home/promano`; no generated command was executed and apply remains denied.
- Phase 1 step 52 is complete as the accepted restarted GenInitrd/GRUB ownership boundary for `6.18.42`. The real `pcold-slack` run passed all 11 assertions, preserved package, boot, active GenInitrd policy, and GRUB state, accepted the one-line evidence-local `AUTO_UPDATE_GRUB=false` staging strategy, and produced verified archive SHA-256 `f906211517c8887e52b2842ff8756973bf9ef5fa4af378a6c830226befe1d522`. The reviewed twelve-stage plan and five recovery boundaries remain non-executing, with `apply_ready=false` and `apply_authorized=false`.
- Phase 1 step 53 is complete as a diagnostic boundary. The real readiness run revalidated the exact 69-candidate digest and preserved package plus boot-sensitive state, but correctly blocked readiness after discovering `/boot/initrd-generic.img -> initrd-6.18.40.img`. The versioned initrd predates the reviewed chain and the active GRUB configuration was regenerated after it, proving that the earlier `direct-generic-no-initrd` classification omitted valid GenInitrd artifacts. Outer archive SHA-256 `94e95acd734c6a183212baa321c037dcfe70e23a4b1c88136e8b90190965a289` and nested normal-update SHA-256 `849ed04fc33d3375d590498b9e933dfb098adae89b07577cc7495a64becaa32f` are diagnostic only; apply remains blocked.
- Phase 1 step 54 is complete as a diagnostic boundary. The real corrected baseline run classified the host as `geninitrd-managed-versioned-initrd`, preserved the package database and boot state, and produced verified archive SHA-256 `16ebd14b3dd3447c6663fb25796c603738ce58f99bff56334813f72d5b4fd2bb`, but the safe-file helper emitted `line 309: [: : integer expected` and rejected the layout. The global restricted `IFS` prevented space-separated `stat` fields from populating `uid` and `gid`; the boot artifacts themselves were unchanged and remain diagnostic only.
- Phase 1 step 55 is complete as the accepted corrected GenInitrd boot baseline for `6.18.42`. The real `pcold-slack` rerun passed all 20 assertions, classified the host as `geninitrd-managed-versioned-initrd`, verified `/boot/initrd-generic.img -> initrd-6.18.40.img`, accepted versioned-initrd SHA-256 `0da0e0289d93cdf2d3b78288bfa23db4c9437b576563f92889399b2c98294442`, preserved package and boot state, and produced verified archive SHA-256 `6429fd626973b0c3fc498642e1cd9230bc0eceb0291e232b515fef625467c6ac`. Apply remains denied.
- Phase 1 step 56 is complete as the accepted corrected kernel-chain restart boundary. The real `pcold-slack` run passed all 20 nested boot assertions and all 6 wrapper assertions, preserved package and boot state, retained `geninitrd-managed-versioned-initrd`, and produced verified outer archive SHA-256 `e48c528aafd9daef64edae721ddc96c9b5c52daac1a6f563fe447d1a66e6996e` plus nested archive SHA-256 `286035b2f88b3bd575a6731171180743d80b79f75c2ccbb138df647a6527fcda`. Apply remains denied.
- Phase 1 step 57 is complete as the accepted rebuilt exact-package boundary for `6.18.42`. The real `pcold-slack` run passed all 13 assertions, revalidated the corrected `geninitrd-managed-versioned-initrd` baseline, inspected only `kernel-generic-6.18.42-x86_64-1.txz`, preserved package and boot state, and produced verified archive SHA-256 `9f702e85a8ff3eb6155b834ed11cfe494ec60f04082914d80bae2a13d02e016f`. The package SHA-256 remains `e9e7a1c5c71c945ee99595868aa8fee8a644b56601ece0c3e5696d643fe84878`; apply remains denied.
- Phase 1 step 58 is complete as the accepted rebuilt GenInitrd policy boundary. The real `pcold-slack` run passed all 11 assertions, revalidated the corrected versioned-initrd baseline, predicted `/boot/initrd-6.18.42.img`, classified the transition as `versioned-to-versioned-initrd`, preserved package plus boot-policy state, and produced verified archive SHA-256 `de4ad831efda30eaaa6a0ee8fc099815cf0bdb9882bded361942dc6719a88e80`. No package, initrd, hook, or GRUB command ran, and apply remains denied.
- Phase 1 step 59 is complete as the accepted rebuilt DKMS-hook boundary. The real `pcold-slack` run passed all 11 assertions, revalidated the corrected `geninitrd-managed-versioned-initrd` baseline, confirmed DKMS 3.4.1 with zero status rows and empty build state, preserved package, boot, hook, and DKMS state, and produced verified archive SHA-256 `780c56432d7d1b1bd4014a56709d6693a7ac1bf1148a072fbf8d1cfceac1cd2f`. Both reviewed hooks remain explicit no-ops and apply remains denied.
- Phase 1 step 60 is complete as the accepted rebuilt command-output-only GenInitrd boundary. The real `pcold-slack` run passed all 12 assertions, revalidated the corrected versioned-initrd baseline, verified the exact cached `kernel-generic-6.18.42` package, projected the reviewed 18-module vector to `/boot/initrd-6.18.42.img`, preserved package plus generator-sensitive state, and produced verified archive SHA-256 `754ebe19080417c9bcd79ba8ca586085c808e24d1d5120286fff19a09c2cf0f0`. Neither command vector, any package operation, initrd generation, hook, nor GRUB mutation was executed; apply remains denied.
- Phase 1 step 61 is complete as a diagnostic-only boundary. The real `pcold-slack` run validated the seven corrected accepted records and the `6.18.40` to `6.18.42` predecessor relationship, then stopped before the initial state capture because `capture_sensitive_state()` referenced undeclared `GENERATOR_SCRIPT` under `set -u`. No evidence archive was published and no package, initrd, GenInitrd, DKMS, or GRUB command was reached; apply remained unavailable.
- Phase 1 step 62 is complete as the accepted corrected GenInitrd/GRUB ownership boundary. The real `pcold-slack` rerun passed all 12 assertions, revalidated the corrected versioned-initrd baseline, selected `temporary-atomic-policy-override`, preserved packages plus all GRUB-sensitive state, and produced verified archive SHA-256 `53acb06384b4a8fbea1feceb73e6aa2381c43f5702a41ce990d0f515d04588fe`. The evidence contains the non-executed twelve-stage plan and five recovery boundaries; apply remains denied.
- Phase 1 step 63 is complete as a blocked readiness diagnostic. The real `pcold-slack` run preserved packages and all transaction-sensitive state, verified the nested evidence archives, and retained the corrected GenInitrd, DKMS, package-cache, and GRUB ownership boundaries, but Slackpkg metadata expanded from the reviewed 69 candidates to 137. The fresh digest is `27eb06d282b4279f90f422235363c36897ff45f334607c00287384b848a8d926`; the exact `6.18.42` kernel transaction is unchanged and the 68 additions are userspace packages, primarily Plasma 6.7.4. Readiness correctly remained blocked with `apply_ready=false` and `apply_authorized=false`; outer archive SHA-256 `1726766092ce5b4b334ba314566ea290d85aa7aa9a563a753cb7262d96a7a69e` and nested archive SHA-256 `cd9ab4ec8c9485e5c196c83d34c0077d0b14651b89afe86a24d7c205a188fbfc` were verified in `/home/promano`.
- The console-reported diagnostic archive from the rejected first run is `slackware-15.0-elilo-oldkernel-retention-preflight-20260801T202118Z.tar.gz` with SHA-256 `ca2b00a8c5521116a2d2eec97df661fe1e460ef742087b1df8ed75605d29e061`. It is diagnostic only and is not an accepted retention result.
- Continuation context: the accepted fresh Slackware-current chain archive is `slackware-current-candidate-chain-refresh-preflight-20260804T210844Z.tar.gz` with SHA-256 `f9be7efe4a46c72f070f63c4278600e6c75547fe05912f038a9d421c8049c7f7`; its exact 69-candidate digest is `918ded076efb3ff0131b296ceae8854765dd5e92cc433542c498276f9aeba3f9` and its target is `6.18.42`. The rejected first restart archive is `slackware-current-current-kernel-chain-restart-preflight-20260804T213533Z.tar.gz` with SHA-256 `ea7b0d7fa6ff5f9020f41ae06f5bea5c91a79d579dddf1bc25d258ca484605d1`; it is diagnostic only. All target-bound `6.18.41` evidence is historical and must not authorize apply. The original step-46 through step-52 chain is historical only because it depended on `direct-generic-no-initrd`; none of those records may authorize apply. The step-53 readiness archive is diagnostic only because it exposed the pre-existing versioned initrd. The first step 54 corrected boot-baseline archive is diagnostic only with SHA-256 `16ebd14b3dd3447c6663fb25796c603738ce58f99bff56334813f72d5b4fd2bb` because restricted-IFS metadata parsing left UID/GID empty. The corrected step-55 boot baseline is accepted with archive SHA-256 `6429fd626973b0c3fc498642e1cd9230bc0eceb0291e232b515fef625467c6ac`. The corrected step-56 chain restart is accepted with outer archive SHA-256 `e48c528aafd9daef64edae721ddc96c9b5c52daac1a6f563fe447d1a66e6996e` and nested boot archive SHA-256 `286035b2f88b3bd575a6731171180743d80b79f75c2ccbb138df647a6527fcda`. The rebuilt step-57 package inspection, step-58 GenInitrd policy, and step-59 DKMS-hook inspection are accepted. The accepted step-58 policy archive SHA-256 is `de4ad831efda30eaaa6a0ee8fc099815cf0bdb9882bded361942dc6719a88e80`; the accepted step-59 DKMS archive SHA-256 is `780c56432d7d1b1bd4014a56709d6693a7ac1bf1148a072fbf8d1cfceac1cd2f`. Continue only with the corrected step-62 GenInitrd/GRUB ownership preflight. The first step-61 run produced no archive because it stopped during initial state capture on an undeclared-variable error; it is console diagnostic only. All older step-46 through step-52 records tied to `direct-generic-no-initrd` remain invalid for apply. The accepted retention baseline archive is `slackware-15.0-elilo-oldkernel-retention-preflight-20260801T204121Z.tar.gz` with SHA-256 `5afedf07c964369e19ed7ba28f89f2c92caf50a1f46bba813f5652baedc7c3b4`. Repeat the corrected retention preflight no earlier than `2026-08-08T19:51:00+02:00`; the distinct later-boot requirement is already satisfied. The Slackware 15.0 ELILO VM remains frozen. Copy every generated `.tar.gz` and `.sha256` directly to `/home/promano` with `promano` ownership.

### Fixed

- The Slackware-current recovery path now distinguishes a completed package transaction followed by boot-orchestration failure from an incomplete package update. The exact installed post-state is retained instead of restarting candidate review or reinstalling packages.
- Post-apply boot validation now accepts the unchanged, previously reviewed GRUB menuentry pairing `/boot/vmlinuz-generic` with `/boot/initrd-generic.img` after both links have moved coherently to the exact 6.18.42 artifacts; no unnecessary GRUB mutation is performed.
- Rollback reporting no longer claims that the 6.18.40 disk kernel and initrd survived the package upgrade when only the running session and old module tree remain.
- Slackware-current normal-update apply can now accept either the historical apply-ready boot record or the final transaction-readiness record. The readiness path requires the exact accepted archive and refreshed candidate digests, `readiness_status=apply-ready`, `pause_safe=false`, `apply_authorized=false`, and the explicit authorization-review next stage; stale or falsely pause-safe records fail closed.
- GenInitrd/GRUB ownership sensitive-state capture now references the declared `GENINITRD_SCRIPT` instead of the nonexistent `GENERATOR_SCRIPT`. The ownership harness executes `capture_sensitive_state()` under `set -u`, verifies both declared script paths in the captured inventory, and rejects reintroduction of the undeclared name.
- The GRUB atomic-replacement harness now isolates `PATH` while simulating an unavailable `grub-script-check`, so the fail-closed branch remains deterministic even when the host running the tests has a real validator binary installed.
- The exact Slackware-current kernel-package preflight now binds the accepted `geninitrd-managed-versioned-initrd` boot and chain-restart records instead of the revoked direct-no-initrd records. Before any download it verifies the live generic-kernel hash, named initrd link, versioned-initrd hash and size, GenInitrd symlink policy, active GRUB hash, and same-menuentry kernel/initrd pairing.
- Package-preflight boot immutability now includes `/etc/default/geninitrd`, `/boot/initrd-generic.img`, the versioned initrd, GenInitrd scripts, active hooks, and GRUB state. Evidence-local `grub-mkconfig` output must preserve the generic-kernel and named-initrd pair.
- GenInitrd versioned-initrd validation no longer parses space-separated `stat` output under the script-wide newline/tab-only `IFS`. It uses colon-delimited metadata with a local parser, rejects empty or malformed fields before arithmetic, and avoids shell integer diagnostics while preserving root ownership and non-writable mode requirements.
- Slackware-current boot-layout discovery no longer fails when package-list metadata identifies one exact target kernel but the local Slackpkg file inventory has not yet published the target `vmlinuz` member. It records either `present` or `deferred-to-exact-package-preflight`; the exact downloaded `.txz` remains the mandatory ownership source before apply readiness.
- The kernel-chain restart wrapper now validates and republishes the nested target-image metadata state, corrected `geninitrd-managed-versioned-initrd` mode, versioned-initrd SHA-256, and GenInitrd transition requirement. It rejects the revoked direct-no-initrd baseline and keeps the exact-package preflight as the next stage.
- The Slackware-current boot preflight success message no longer hard-codes the historical 57-candidate count. It now describes the accepted record generically, so restarted chains for later exact candidate sets cannot publish misleading evidence text.
- The reference boot module now distinguishes `mkinitrd-managed`, validated `direct-generic-no-initrd`, and partial layouts. On the direct Slackware-current path, `kernel-generic` changes no longer create an impossible initrd requirement; initrd execution is suppressed, but GRUB remains mandatory and is blocked until the new package-owned `vmlinuz-VERSION` target plus `/lib/modules/VERSION` pass post-update validation.
- Direct generic boot detection now parses exactly one safe `BOOT_IMAGE`, requires an exact GRUB `linux` or `linuxefi` reference rather than a comment or substring, resolves the `/var/log/packages` compatibility path canonically, rejects ambiguous or symlinked package records, and ties both the running kernel image and module tree to one installed `kernel-generic` record.
- Generated GRUB configurations for direct generic transitions must reference either the validated versioned kernel or the validated generic symlink. Syntax-valid but stale configurations are rejected before atomic replacement, and the structured result exposes the preparation layout, direct-kernel validation status, resolved kernel path, module path, and failure reason.
- Slackware-current kernel boot discovery no longer requires an initrd unconditionally. A coherent direct GRUB boot is accepted only when `mkinitrd.conf` and `initrd.gz` are both absent, `/boot/vmlinuz-generic` resolves to the package-owned running `vmlinuz-VERSION`, one exact target package trio exists, and the running `BOOT_IMAGE` is present in a syntax-valid GRUB configuration. Target file inventory is consumed when available and otherwise deferred explicitly to the exact-package preflight. Mixed states still fail closed.
- Slackware-current normal-update apply now requires a matching accepted boot-layout preflight whenever the refreshed candidate set contains kernel packages. `--allow-kernel-update` alone is no longer sufficient: the separately supplied evidence SHA-256 must identify an accepted record for the same candidate digest with `apply_ready=true` and `apply_authorized=false`. Missing, stale, mismatched, or discovery-only evidence blocks before package installation.
- Normal-update candidate extraction now recognizes Slackware package filenames whose architecture field is exactly `x86`, including `kernel-headers`. The 2026-08-03 diagnostic proved that omitting this tag understated the upgrade count, kernel count, and candidate-set SHA-256; such incomplete evidence is now explicitly rejected and a fresh preflight is required.
- Normal-update evidence sidecars now store only the archive basename and the scenario prints a destination-side `sha256sum -c` command, so the copied archive and sidecar verify directly from `/home/promano` instead of referencing the private `/var/tmp` source path.
- Evidence SHA-256 sidecars from the ELILO oldkernel retention preflight now record only the archive basename. After the archive and sidecar are copied together to `/home/promano`, `sha256sum -c` works from that destination without trying to reopen the private source path under `/var/tmp`; the script also prints the exact verification command.
- The ELILO oldkernel retention preflight now resolves the supported Slackware `/var/log/packages` compatibility symlink to its canonical package database before enumeration. It still rejects broken links, non-directory destinations, unreadable databases, package-record symlinks, duplicate retained kernel records, and unsafe package-log paths.
- Package and boot immutability checks now compare only four successfully captured regular files. Missing or partial captures can no longer trigger unconditional `sha256sum` errors or produce false unchanged-state passes from empty hash values.
- Cleanup eligibility is now evaluated only after the final package and boot captures are complete and byte-identical to their initial state. Any capture, comparison, retention, or later-boot failure forces `cleanup_eligible=false` before the cleanup plan is written.
- Replaced the unsupported single extended-regex argument to `slackpkg download` with three exact package-name invocations: `kernel-generic`, `kernel-huge`, and `kernel-modules`. Each raw status is recorded separately, the sequence stops at the first failure, and the existing cache resolver still accepts only the exact reviewed version, architecture, build, and repository path.

- Phase 1 step 64 is prepared as the corrected candidate-chain classification boundary. The refresh wrapper now compares the exact generic, headers, and source transaction instead of treating every digest change as a kernel change; it uses the accepted 69-candidate record by default, verifies configured critical candidates, identifies strict userspace supersets, and exposes whether the corrected kernel evidence may be explicitly rebound only after userspace review. The readiness summary now returns blocked candidate drift to `current-candidate-chain-refresh-preflight` rather than incorrectly naming apply authorization. No package, initrd, DKMS, or GRUB action is available.
### Added

- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-kernel-post-apply-reboot-review-20260805-accepted.json` as the immutable accepted record for the 13-pass real reboot review, exact evidence digest, code and policy identities, effective GRUB selection, unchanged host state, degraded rollback acknowledgement, and one authorized but not yet executed manual reboot.
- Added `tests/acceptance/reference/test-current-kernel-post-apply-reboot-review.sh`, a read-only Slackware-current boundary that authorizes only a later manual reboot after revalidating the accepted local checkpoint. It rejects package or boot drift, a changed current boot path, GRUB syntax or digest changes, nonempty `next_entry`, ambiguous defaults, and any effective selection that does not pair `/boot/vmlinuz-generic` with `/boot/initrd-generic.img`; it records selector, entry title, and entry ID in structured evidence.
- Added `tests/reference/test-current-kernel-post-apply-reboot-review-harness.sh` with 68 checks covering zero mutation, exact checkpoint and authorization binding, literal defaults, `saved_entry`, generated menuentry IDs, numeric submenu paths, package and artifact drift, GRUB environment overrides, unsupported selectors, degraded rollback acknowledgement, and fail-closed pause-safety separation.
- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-kernel-post-apply-reboot-review-policy.json`, binding the accepted safe-pause archive, checkpoint, recovery policy and code, reboot-review code, host identity, target 6.18.42, degraded rollback truth, forbidden repository refresh and host mutation, and explicit authorization scope SHA-256 `01739dc15e197ce7a4d15e749ca2e0b844de3900e39be83413ad7e5ecdc2307c`.
- Added `tests/acceptance/reference/test-current-post-package-boot-recovery-verification.sh`, an exact non-mutating Slackware-current post-package boundary that binds the completed 2,040-record transaction, target 6.18.42 kernel/initrd/modules, generic links, restored GenInitrd controls, unchanged accepted GRUB digest, same-menuentry generic pair, and degraded 6.18.40 rollback state before reporting a genuinely safe pause.
- Added `tests/reference/test-current-post-package-boot-recovery-verification-harness.sh` with synthetic coverage for exact post-state acceptance, package drift, target record drift, artifact hashes, link targets, module trees, GRUB syntax and pairing, GenInitrd identities, rollback-state mismatches, zero system mutation, structured evidence, and fail-closed pause safety.
- Added the rejected step-78 post-package diagnostic and the exact step-79 recovery policy, preserving the completed Slackpkg transaction while separating safe-pause verification from reboot authorization.
- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-userspace-payload-review-20260805-boot-prefix-diagnostic.json` to preserve the rejected 13-pass/two-failure real payload review, exact outer and nested archive digests, exact Breeze package identity, stale-versus-corrected theme prefix, unchanged host state, and mandatory corrected rerun.
- Corrected the userspace payload policy and inspector for Slackware's exact `boot/grub/themes/breeze/` package layout. The exception now requires the reviewed Breeze archive filename, SHA-256, and byte size; permits only the three ancestor directories plus the exact theme root and subtree; and retains fail-closed rejection for all other `/boot` and GRUB content. The focused harness now exercises the real explicit-directory layout plus archive identity drift, wrong-package paths, stale `usr/share` paths, sibling themes, and extra boot payloads.
- Added `tests/acceptance/reference/test-current-kernel-evidence-rebind-preflight.sh`, its 62-check focused harness, the accepted step-65 userspace-review record, and an explicit reviewed rebind policy. The preflight verifies all seven accepted kernel evidence archives, reruns only the normal-update `--preflight`, validates the exact 137-candidate set, rechecks live GenInitrd/DKMS/GRUB invariants, and emits an evidence-local candidate-binding map without executing packages or boot mutations.
- Added `tests/acceptance/reference/test-current-userspace-candidate-review-preflight.sh`, its focused harness, the accepted step-64 refresh record, and an explicit reviewed policy for the 68-package userspace expansion. The preflight reruns only the normal-update `--preflight`, verifies its nested portable archive, rechecks exact candidate identity and category union, proves the kernel transaction remains unchanged, captures package and boot state before and after, and routes only to a separate kernel-evidence rebind stage.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-transaction-readiness-20260805-userspace-expansion-diagnostic.json` for the verified 137-candidate readiness stop, both archive digests, exact 68-package addition list, unchanged `6.18.42` kernel transaction, unchanged live state, and immutable apply denial.
- Corrected `test-current-candidate-chain-refresh-preflight.sh` to use the accepted 69-candidate baseline, compare exact kernel package identities, distinguish same-kernel userspace expansion from a changed kernel target, validate configured critical candidates, and report strict-superset plus explicit kernel-evidence rebind eligibility.
- Corrected blocked readiness output so candidate drift selects `current-candidate-chain-refresh-preflight`; only a clean apply-ready result selects the separate authorization review.
- Preserved a syntactically valid fresh candidate digest in blocked readiness evidence and console summaries even when the exact accepted-set comparison fails, while malformed or ambiguous digest fields remain hidden.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-package-preflight-20260805-accepted.json` for the accepted 13-pass rebuilt package inspection, corrected GenInitrd baseline identity, exact package and evidence digests, unchanged host state, and immutable apply denial.
- Rebuilt `test-current-geninitrd-policy-preflight.sh` against the corrected GenInitrd chain. It rejects the revoked direct-boot records, revalidates the live versioned-initrd baseline before policy analysis, models a versioned-to-versioned target transition, and keeps all generated or mutation commands unavailable.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-chain-restart-20260805-accepted.json` for the accepted corrected chain restart, both portable archive digests, accepted step-55 baseline identity, exact versioned-initrd SHA-256, unchanged host state, revoked direct mode, and immutable apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-boot-preflight-20260805-stat-ifs-diagnostic.json` for the verified 19-pass/1-failure step-54 run, exact archive and boot-artifact hashes, parser root cause, unchanged host state, and mandatory corrected rerun.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-package-preflight-20260804-accepted.json` for the reviewed `6.18.42` package archive, accepted candidate/boot/restart identities, exact package and evidence digests, safe member inventory, conditional GenInitrd hook, unchanged host state, and immutable apply denial.
- Restarted `test-current-geninitrd-policy-preflight.sh` against the accepted 2026-08-04 chain. It now validates all four candidate, boot, chain-restart, and package records before host-policy inspection and rejects digest, target, evidence, package, or authorization drift.

- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-chain-restart-20260804-diagnostic.json` to preserve the rejected 19-pass/1-failure nested boot discovery, the 4-pass/2-failure outer result, both portable archive digests, unchanged system state, and the corrected ownership boundary.
- Added `tests/acceptance/reference/test-current-kernel-chain-restart-preflight.sh` as the target-bound step 45 wrapper. It validates the accepted 2026-08-04 refresh plus normal-update records, derives the exact `6.18.42` target and 69-candidate digest, invokes only `test-current-kernel-boot-preflight.sh`, nests and verifies its evidence, and permanently denies package, initrd, GRUB, readiness, and authorization operations.
- Added `tests/reference/test-current-kernel-chain-restart-preflight-harness.sh` with 43 focused checks covering accepted-record binding, digest reconstruction, complete kernel companions, unsafe and overlapping candidates, stale targets, nested summary identity, portable nested archives, immutable apply denial, and outer evidence metadata.
- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-preflight-20260804-accepted.json` and `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-candidate-chain-refresh-20260804-accepted.json` for the reviewed 69-candidate baseline, target `6.18.42`, accepted archive digests, unchanged package/boot state, and explicit chain restart boundary.

- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-direct-generic-preflight-20260803-accepted.json` for the reviewed 20-pass real-system evidence, portable archive digest, accepted direct boot mode, unchanged package and boot state, executed and hardened reference hashes, and immutable apply denial.
- Added `tests/reference/test-current-direct-generic-boot-policy.sh` with 56 focused checks covering compatibility package-database symlinks, exact package ownership, safe `BOOT_IMAGE` parsing, exact GRUB command references, direct-versus-managed policy selection, post-update symlink and module validation, generated-GRUB target validation, and boot-safe result mapping.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-direct-generic-preflight-20260803-diagnostic.json` to preserve the rejected 16-pass/3-failure real-system discovery, its portable archive digest, unchanged package and boot state, the observed direct-generic layout, and explicit apply denial.
- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-preflight-20260803-accepted.json` as the sanitized accepted classification record for the corrected 57-candidate preflight. It records portable destination verification, exact candidate identity, unchanged package/boot state, the target kernel `6.18.41`, and explicit apply denial pending boot-layout review.
- Added `tests/acceptance/reference/test-current-kernel-boot-preflight.sh` as a Slackware-current-only, non-destructive discovery stage. It validates the monolithic `kernel-generic` package layout, exact repository transition, module ownership, mkinitrd configuration, generic-kernel symlink, initrd, GRUB state, and before/after immutability; it always emits `apply_ready=false` and `apply_authorized=false`.
- Added `tests/reference/test-current-kernel-boot-preflight-harness.sh` with 62 focused checks covering argument and identity validation, non-destructive source boundaries, safe kernel-version and image parsing, direct-generic versus managed-initrd classification, exact package ownership, accepted and diagnostic fixtures, portable evidence handling, and immutable apply denial.
- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-preflight-20260803-parser-diagnostic.json` as the sanitized rejected record for the 2026-08-03 preflight. It preserves the raw/normalized count mismatch, omitted `kernel-headers` candidate, reconstructed 57-package digest, unchanged-system evidence, non-portable original sidecar, and explicit apply denial.
- Added `tools/reference/kernel-cleanup-dry-run-reference.sh` as the phase 1 step 32 simulation-only executor. It requires an explicit `--dry-run` flag, validates the plan SHA-256 and immutable apply denial, accepts only a matching schema-1 authorization with `scope=dry-run-only` and `apply_authorized=false`, emits deterministic proposed command vectors, supports deterministic failure injection plus recovery planning, and never invokes package tools, edits boot configuration, deletes files, creates evidence directories, or exposes a real apply mode.
- Extended cleanup plans with normalized `boot_transaction` metadata: exact ELILO or GRUB configuration paths, backend state, and only the two legacy ELILO EFI files that may eventually become unreferenced. GRUB plans explicitly carry an empty unowned-artifact list.
- Added `tests/reference/test-kernel-cleanup-dry-run.sh` with 103 focused checks covering mandatory dry-run mode, plan and authorization identity, ineligible and unauthenticated blocking, single-kernel no-op behavior, complete ELILO and GRUB simulations, zero execution/mutation guarantees, deterministic output, source execution guards, exact command-vector rendering, failure injection, recovery boundaries, unsafe path/package/action rejection, and strict simulation-only authorization matching.
- Added a synthetic mature ELILO fixture and a schema-1 dry-run authorization policy fixture. They exist only to exercise the simulator and do not represent mature real-system eligibility or cleanup authorization.

- Added `tools/reference/kernel-cleanup-plan-reference.sh` as the phase 1 step 31 plan-only contract. It validates schema-1 cleanup inventories for ELILO and GRUB, requires the running kernel to match the active kernel, requires exact `kernel-generic`, `kernel-huge`, and `kernel-modules` active/rollback triples, requires complete active-package archive coverage before a rollback plan can exist, preserves kernel headers/source/firmware packages, emits deterministic JSON plus a plan SHA-256, and never authorizes or executes apply.
- Added `tests/reference/test-kernel-cleanup-plan.sh` with 97 focused checks covering deterministic output, CLI/path boundaries, exact package sets, active archive coverage, module-tree requirements, package-database safety, ELILO versioned active entries, atomic oldkernel-removal planning, GRUB staged regeneration, single-kernel no-op behavior, unsafe inventory rejection, and destructive-command source guards.
- Added three schema-1 cleanup inventory fixtures: an ELILO dual-kernel design derived from the accepted retention evidence, a synthetic mature GRUB dual-kernel design, and the console-observed Slackware 15.0 GRUB development VM baseline. The observed VM runs `5.15.209` from `vmlinuz-huge-5.15.209`, has only the active boot-kernel package triple, keeps `kernel-headers`, `kernel-source`, and `kernel-firmware`, and therefore correctly produces a non-applicable zero-action cleanup result.

- Added `tests/acceptance/reference/test-elilo-oldkernel-retention-preflight.sh` as the non-destructive phase 1 step 30 boundary. It loads the accepted transaction record, requires a seven-day retention interval and one later boot after the accepted reboot review, validates the exact active and rollback package sets, verifies both ELILO entries and their `/boot` plus EFI artifacts, captures the boot ID, inventories shared old/new package paths, proves package and boot state remain unchanged, and always records `cleanup_authorized=false`.
- Added `tests/reference/test-elilo-oldkernel-retention-preflight-harness.sh` with 109 focused checks covering argument and version validation, accepted-record loading, retention timing, later-boot detection, exact two-entry ELILO parsing, Slackware package-database compatibility symlinks, six-record kernel-package coexistence, package-record symlink rejection, package-path overlap inspection, unsafe and duplicate rejection, complete-state comparison, post-comparison eligibility gating, ordered cleanup planning, portable basename-only SHA-256 sidecars, destination-directory verification, direct `/home/promano` evidence publication, destructive-command source guards, and both retention fixtures.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-oldkernel-retention-policy.json` to preserve the seven-day and two-boot thresholds, the non-destructive eligibility boundary, the ordered future cleanup sequence, and explicit cleanup denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-oldkernel-retention-preflight-accepted.json` for the reviewed 24-pass real-system baseline, accepted archive digest, canonical package-database resolution, later successful boot, unchanged package and boot state, unmet seven-day window, earliest eligibility boundary, and explicit cleanup denial.

- Recorded the accepted Slackware 15.0 ELILO kernel transaction and post-reboot validation: the reviewed apply archive passed 31 assertions with SHA-256 `93c58c1508085f3dffaa182eac52fb49c72e6222d75b49c79af4309713f0ac95`, the machine rebooted into `5.15.209`, `/proc/cmdline` selected the versioned EFI kernel, and the `oldkernel` rollback entry remained available.
- Added `slackware-15.0-elilo-kernel-transaction-accepted.json` as the sanitized acceptance record for the completed apply and reboot boundary.

- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-transaction-apply-download-diagnostic.json` for the reviewed safe-stop evidence: Slackpkg status `20`, zero installed packages, byte-identical blacklist restoration, unchanged working boot artifacts, and explicit `retry_authorized=false`.
- Added `tests/acceptance/reference/test-elilo-kernel-transaction-apply.sh` as the explicitly gated ELILO kernel apply stage. It revalidates hostname, target version, and the exact reviewed candidate digest after a fresh metadata update; uses `slackpkg download` only to fetch the three exact kernel packages; installs them with `installpkg` so the working kernel remains installed; restores the Slackpkg blacklist byte-for-byte; parses the generated `mkinitrd` command without `eval`; creates a versioned initrd; verifies versioned kernel and initrd copies in the EFI partition; preserves an explicit `oldkernel` rollback stanza; and atomically activates the new default through `elilo.conf` without running `eliloconfig` or changing firmware variables.
- Added `tests/reference/test-elilo-kernel-transaction-apply-harness.sh` with 70 focused checks covering mandatory confirmations, exact blacklist editing, cached-package resolution, old/new package coexistence, strict non-evaluating mkinitrd parsing, two-entry ELILO generation, explicit default and rollback labels, atomic copies and replacements, command boundaries, source audits, and accepted-preflight metadata.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-transaction-preflight-accepted.json` for the reviewed 27-pass real-system plan, target `5.15.209`, candidate digest, conservative space results, and unchanged-system boundary.
- Added `tests/acceptance/reference/test-elilo-kernel-transaction-preflight.sh` as the third non-destructive ELILO stage. It refreshes metadata, resolves one complete common `kernel-generic`, `kernel-huge`, and `kernel-modules` repository candidate directly from Slackpkg metadata, computes the exact selected-record digest, rejects mixed or ambiguous versions, validates the accepted current generic-kernel and initrd mappings, checks conservative `/boot` and EFI free space, and writes a versioned `elilo.conf` plan without changing packages, blacklist entries, initrd images, EFI files, or firmware variables.
- Added `tests/reference/test-elilo-kernel-transaction-preflight-harness.sh` with 72 focused checks covering non-destructive source guards, target and path validation, safe kernel-version comparison, exact installed-record parsing, newest complete patches-candidate selection across retained historical versions, incomplete and duplicate set rejection, portable free-space parsing, planned ELILO rewriting, unrelated-setting preservation, accepted and diagnostic evidence validation, and publication boundaries.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-transaction-preflight-diagnostic.json` for the reviewed non-destructive run that exposed retained patch-history selection and Slackware 15.0 `df` compatibility defects.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-source-accepted.json` for the accepted third real-system run and `pkglist-kernel-candidates.tsv` as a deterministic representative Slackpkg metadata fixture.
- Added `tests/acceptance/reference/test-elilo-generator-preflight.sh` as the second non-destructive Slackware 15.0 ELILO stage. It validates UEFI and ELILO boundaries, records root and EFI mounts, requires one safe `image` and `initrd` basename, maps the EFI kernel copy to one unique versioned `/boot/vmlinuz-*` source, compares the initrd copies, invokes the official `mkinitrd_command_generator.sh` only to capture its proposed command for the running kernel, and proves that packages plus boot artifacts remain unchanged. The generated command is never evaluated or executed.
- Added `tests/reference/test-elilo-generator-preflight-harness.sh` with 65 focused checks covering source-level non-execution guards, exact target and path validation, strict ELILO directive parsing, traversal rejection, package-database symlink handling, unique versioned-kernel matching, alias deduplication, ambiguous and stale-source rejection, reviewed-fixture validation, and evidence publication.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-discovery-reviewed.json` to preserve the reviewed real-system discovery, archive digest, UEFI/ELILO entry, unresolved initial kernel-source mapping, matching initrd copy, absent `mkinitrd.conf`, deferred kernel packages, and explicit `apply_authorized=false` boundary.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-15.0-elilo-generator-reviewed.json` for the second real-system run. It preserves the valid non-executed generator command, the observed `/boot/vmlinuz` huge-kernel alias mismatch, the matching initrd copy, the deferred kernel packages, and the next required versioned-source inventory boundary.
- The ELILO generator preflight now inventories every top-level `/boot/vmlinuz*` file and symlink, records resolved paths and hashes, collapses aliases that resolve to the same file, and requires exactly one matching versioned generic or huge kernel for the running release. It no longer treats the generic `/boot/vmlinuz` alias as the authoritative ELILO source.
- The kernel boot-path preflight now treats an absent `/etc/mkinitrd.conf` as `generator-available` when the official Slackware command generator is a readable regular file. This is a successful discovery state, not permission to update the kernel.
- Added `tests/acceptance/reference/test-kernel-boot-preflight.sh` for the dedicated Slackware 15.0 boot-kernel scenario. The preflight never changes packages, the Slackpkg blacklist, initrd images, or boot-loader files; it records firmware mode, probable LILO/ELILO/GRUB classification, current reference support, `mkinitrd.conf` scalar settings, installed and repository kernel records, deferred blacklist entries, boot artifacts, and byte-stable before/after evidence.
- Added `tests/reference/test-kernel-boot-preflight-harness.sh` with focused coverage for target and path validation, non-destructive source guards, BIOS/UEFI and loader evidence boundaries, safe scalar parsing, Slackware package-database compatibility symlinks, blacklist matching, evidence publication, and the one-line `promano` copy command.
- Added `tests/acceptance/reference/test-current-kernel-package-preflight.sh` as a root-only but non-installing Slackware-current transaction inspection stage. It downloads one exact package name, resolves exactly one reviewed cache file, validates the archive without extraction into the host filesystem, records the package SHA-256 and complete member inventory, extracts `doinst.sh` only into private evidence, and preserves package plus boot fingerprints before and after inspection.
- Hardened `tests/reference/test-current-kernel-package-preflight-harness.sh` to 48 checks. The package policy now requires exactly one recognized conditional `geninitrd` invocation and records a mandatory host-policy preflight instead of treating the versioned symlink transition as the package script's only boot effect.
- Added `tests/acceptance/reference/test-current-geninitrd-policy-preflight.sh` and `tests/reference/test-current-geninitrd-policy-preflight-harness.sh`. The 46-check harness covers accepted-record binding, strict non-evaluating configuration parsing, script control-flow recognition, generator fallback, disabled autogeneration, automatic GRUB prediction, custom hooks, unsafe symlinks, unsupported configuration, legacy setup skips, portable evidence, and immutable apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-package-preflight-20260803-accepted.json` for the reviewed package archive, package digest, member counts, conditional hook, unchanged host state, and explicit next-stage boundary.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-policy-preflight-20260803-accepted.json` for the reviewed effective generator, automatic initrd and GRUB transition, exact executable DKMS hook paths and digests, unchanged host state, and explicit apply denial.
- Added `tests/acceptance/reference/test-current-geninitrd-dkms-hook-preflight.sh` and `tests/reference/test-current-geninitrd-dkms-hook-preflight-harness.sh`. The 51-check harness covers accepted-record binding through the restarted candidate, boot, chain, package, and policy evidence, required kernel generic/header/source candidates, exact hook-set and digest matching, root-owned non-writable executable metadata, syntax checks, no-execution source guards, static command-surface capture, symlink and unexpected-hook rejection, read-only tree inventory, portable evidence, and immutable apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-dkms-hook-preflight-20260803-accepted.json` for the reviewed zero-row DKMS state, exact hook digests, explicit no-op predictions, unchanged host state, portable evidence verification, and immutable apply denial.
- Added `tests/acceptance/reference/test-current-geninitrd-command-preflight.sh` and `tests/reference/test-current-geninitrd-command-preflight-harness.sh`. The 57-check harness covers six-record transaction binding, no-op DKMS prerequisites, root-owned generator and setup scripts, command-output-only invocation, strict inert `mkinitrd` parsing, target-kernel and versioned-initrd projection, shell-operator and traversal rejection, exact cached-package binding, portable evidence, and permanent no-execution/apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-command-preflight-20260803-accepted.json` for the reviewed 11-pass command projection, exact 18-module vector, versioned target initrd, unchanged host state, and explicit no-execution/apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-command-preflight-20260804-accepted.json` for the corrected 11-pass `6.18.42` command evidence, complete archive linkage, exact cached-package identity, 18-module projected vector, unchanged host state, and explicit no-execution/apply denial.
- Added `tests/acceptance/reference/test-current-geninitrd-grub-ownership-preflight.sh` and `tests/reference/test-current-geninitrd-grub-ownership-preflight-harness.sh`. The 55-check harness covers seven-record transaction binding, source-order and guard recognition, exact active policy parsing, evidence-local one-line staging, environment-override rejection, temporary atomic policy ownership, twelve ordered transaction stages, five recovery boundaries, state immutability, portable evidence, and permanent no-execution/apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-grub-ownership-preflight-20260803-accepted.json` for the reviewed 11-pass ownership evidence, exact policy digests, twelve-stage plan, five recovery boundaries, unchanged host state, and explicit apply denial.
- Added `tests/reference/test-geninitrd-grub-ownership-engine.sh` with 58 focused checks covering no-op non-direct layouts, exact one-line staging, ownership and mode preservation, same-directory backup, atomic replacement and restoration, cleanup restoration, symlink and writable-policy rejection, duplicate or unexpected assignment rejection, concurrent-change refusal, backup-drift refusal, workflow ordering, structured reporting, and accepted evidence integrity.
- The reference engine now owns the GenInitrd/GRUB boundary during a direct-generic Slackware-current package transaction. It activates the reviewed temporary policy before `slackpkg`, restores it before post-update analysis, fails package processing when restoration cannot be proven, retains recovery material on conflict, and exposes all ownership states in JSON.
- Added `tests/reference/test-current-geninitrd-post-state.sh` and `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-post-state-synthetic.json`. The 46-check synthetic harness covers strict `AUTOGENERATE_INITRD` parsing, exact target-package resolution, package-owned kernel and module trees, safe versioned initrd metadata, exact named-link selection, legacy-initrd exclusion, generated GRUB kernel-plus-initrd requirements, and fail-closed partial states without executing commands or mutating the host.
- The reference engine now records whether GenInitrd output is mandatory before package operations, validates the generated versioned initrd and named symlink after the package snapshot, blocks GRUB on any incomplete transition, and requires the temporary GRUB configuration to reference both the validated target kernel and initrd. Structured output exposes the expected transition, post-state, paths, status, and diagnostics.
- Added `tests/acceptance/reference/test-current-candidate-chain-refresh-preflight.sh` and `tests/reference/test-current-candidate-chain-refresh-preflight-harness.sh`. The 62-check harness covers exact baseline validation, embedded preflight-only invocation, deterministic candidate hashing, operation-list disjointness, count consistency, safe package filenames, complete target kernel companions, changed and unchanged chains, userspace and no-update branches, ambiguous or incomplete targets, state immutability, portable evidence, and permanent apply denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-command-preflight-20260804-diagnostic.json` for the verified 10-pass/1-failure real-system run, stale `6.18.41` digest diagnosis, safe projected command, immutable host state, and mandatory corrected rerun.
- The step-51 correction removes both duplicated package digests from the executable preflight, loads the exact filename and SHA-256 from the accepted step-47 package record, records expected and observed cache digests in evidence, and fails closed when the accepted identity is malformed.
- The step-52 ownership preflight is restarted against the complete `6.18.42` evidence chain. It adds the accepted chain-restart record as a mandatory input, cross-checks every archive link through the accepted command record, and rejects a command record whose package digest differs from exact-package evidence.
- Added `tests/acceptance/reference/test-current-kernel-transaction-readiness-preflight.sh` and `tests/reference/test-current-kernel-transaction-readiness-preflight-harness.sh`. The 48-check harness covers exact eight-record linkage, the target-specific synthetic post-state contract, fresh 69-candidate equality, complete kernel companions, exact package-cache identity, live direct-generic boot, reviewed GenInitrd scripts and policy, no-op DKMS state, GRUB syntax validation, immutable state capture, portable evidence, positive readiness, and permanent authorization denial.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-grub-ownership-preflight-20260804-accepted.json` for the reviewed ownership evidence and `slackware-current-geninitrd-post-state-6.18.42-synthetic.json` for the target-specific post-transaction contract.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-kernel-transaction-readiness-20260805-diagnostic.json` for the verified blocked readiness run, unchanged 69-candidate digest, pre-existing versioned initrd, corrected classification, invalidated dependent evidence chain, and immutable apply denial.
- The Slackware-current boot preflight now captures the named and versioned initrd artifacts, recognizes `geninitrd-managed-versioned-initrd`, validates safe root-owned initrd metadata and the active GenInitrd policy, and requires the running generic kernel plus `/boot/initrd-generic.img` in one GRUB menuentry. Partial named/versioned pairs fail closed.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-dkms-hook-preflight-20260805-accepted.json` for the verified 11-pass rebuilt DKMS boundary, corrected versioned-initrd and GRUB linkage, two accepted no-op hooks, empty DKMS state, unchanged host state, and immutable apply denial.
- Rebuilt `test-current-geninitrd-command-preflight.sh` against the corrected step-55 through step-59 chain. It rejects the revoked direct-no-initrd records, revalidates the live named and versioned initrd plus GRUB pairing, derives the exact package identity only from accepted evidence, and records the `versioned-to-versioned-initrd` projection without executing either command vector.
- Added `tests/fixtures/reference/acceptance/kernel-boot/slackware-current-geninitrd-command-preflight-20260805-accepted.json` for the verified 12-pass rebuilt command boundary, corrected versioned-initrd linkage, exact cached-package identity, accepted 18-module projection, immutable host state, and permanent no-execution/apply denial.
- Rebuilt `test-current-geninitrd-grub-ownership-preflight.sh` against the corrected step-55 through step-60 chain. It rejects the revoked direct-no-initrd model, revalidates the live named and versioned initrd plus same-entry GRUB pairing, preserves the one-line evidence-local policy staging strategy, and records `versioned-to-versioned-initrd` without executing the twelve-stage plan.
- The reference validation inventory now contains 37 suites and 2,521 checks with zero failures for step 63. The rebuilt transaction-readiness harness contributes 64 checks, binds the corrected GenInitrd chain, rejects the revoked direct-no-initrd baseline, and records both the current and expected versioned initrds; the corrected GRUB-ownership harness remains at 71 checks and the GenInitrd-command harness remains at 73 checks. The main reference implementation remains SHA-256 `0dc4a4def9b063b9a598975f46e7458c5771eb8d8603f4fa8bbd9dfc07c4d4c6`, the cleanup-plan suite remains SHA-256 `cbcb168c237ae27d2d37d249afa9fe730b02a9963d33bb0dec8e9696a9ee9286`, and the ELF suite remains SHA-256 `dbf40988eae32190d9faaa7703fd78edfdb5a6dbd99c115f405b15a40b8c49ba`.
- Added `tests/acceptance/reference/test-normal-update.sh` to stage the normal Slackware package-update acceptance scenario. Its mandatory preflight asks `slackpkg install-new` and `upgrade-all` for real candidate lists with dialog disabled and a negative default answer, proves that package and boot state remain unchanged, classifies kernel and critical candidates, and creates portable evidence before any installation is authorized.
- The same scenario provides an explicitly gated apply mode that requires an exact hostname acknowledgement, requires the exact reviewed candidate-set SHA-256, separately authorizes kernel and configured critical candidates, isolates Flatpak, SBo, ELF, and Cinnamon, retains boot preparation in `auto` mode, validates successful stable codes `0`, `4`, or `5`, and requires a real package-database change.
- Added `tests/reference/test-normal-update-acceptance-harness.sh` and representative non-interactive `slackpkg` probe fixtures with focused coverage for candidate extraction, right-to-left package-name classification, critical and kernel detection, configuration isolation, physical-host safety gates, evidence summaries, and the one-line `promano` copy fallback.
- Added a sanitized accepted preflight record for the physical Slackware-current normal-update scenario, including the evidence digest, tested platform, exact ten-package candidate set, unchanged package and boot state, and assertion totals.
- Added `tests/fixtures/reference/acceptance/normal-update/slackware-current-apply-reviewed.json` to preserve the reviewed physical-host transaction, its archive digest, exact package changes, boot-state result, 27-file `.new` observation, and explicit revalidation gate for the hardened reference.
- Added sanitized accepted Slackware 15.0 normal-update preflight and apply records. They preserve both evidence digests, the exact 196-package candidate digest, the 12 `install-new` and 184 `upgrade-all` candidates, five critical candidates, temporary deferral of three boot-kernel packages, package-record growth from 1,554 to 1,566, stable code `4`, unchanged boot state, and the real-system inventory of 45 deferred `.new` files.
- Added `tests/reference/test-slackpkg-postinstall-policy.sh` with focused coverage for `-postinst=off`, exact update/install-new/upgrade-all argument boundaries, deterministic `/etc/*.new` enumeration, human and structured warnings, JSON metadata, and scan failures.
- The normal-update harness now refreshes Slackware metadata before every candidate classification, records the raw refresh status and output, and repeats that boundary immediately before real apply so newly published packages cannot bypass review through stale local lists: any candidate-set digest change blocks apply, and kernel plus configured critical packages retain separate authorization gates.

- Added `tests/acceptance/reference/test-no-updates.sh` as the first real-system Phase 1 acceptance scenario. It requires an explicit apply acknowledgement, verifies the declared Slackware target, proves the no-updates precondition with the real `--check --json` path, runs the isolated real `--apply --json` Slackware workflow, compares byte-level installed-package evidence and selected boot state, and creates a private evidence archive with a SHA-256 sidecar.
- Added `tests/acceptance/reference/README.md` with the safety boundary, execution commands for Slackware 15.0 and Slackware-current, scenario scope, expected result contract, and evidence-sanitization guidance.
- Added expected provisional check and apply fixtures under `tests/fixtures/reference/acceptance/no-updates/` for stable exit code `0`, no reboot guidance, valid unchanged package snapshots, disabled optional modules, and empty warning/error arrays.
- Added sanitized accepted-evidence records for both mandatory targets under `tests/fixtures/reference/acceptance/no-updates/`, including archive digests, tested platforms, raw `slackpkg` statuses, package counts, unchanged boot state, and assertion totals.
- Added `tests/reference/test-no-updates-acceptance-harness.sh` with focused coverage for target validation, explicit apply confirmation, check-before-apply ordering, isolated configuration generation, fixture validation and rejection, deterministic package evidence, actionable structured-failure details, archive contents, caller ownership, private publication modes, and SHA-256 verification.

- Added `tests/reference/test-cron-minimal-environment.sh` with focused coverage for direct and harnessed `env -i` execution, hostile inherited values, deterministic administrative command discovery, detached standard streams, `/dev/null` input, human/JSON/NDJSON output separation, non-interactive `slackpkg` arguments, prompt suppression, and owner-only runtime files.

- Added `tests/reference/test-error-exit-codes.sh` with focused coverage for all stable process statuses `0` through `8`, every current structured-result error source, successful non-zero reboot outcomes, early argument/configuration/privilege/lock failures, runtime and logging initialization failures, completion-timestamp failures, and JSON/NDJSON exit-code propagation.

- Added `tests/reference/test-signal-cleanup.sh` with real subprocess coverage for `SIGHUP`, `SIGINT`, and `SIGTERM`, conventional interruption statuses, active-lock observation, explicit lock release, owned temporary-resource cleanup, unrelated-file preservation, pre-runtime interruption safety, non-resumption, and source-level trap-order guards.
- Added dedicated runtime trap installation, interruption handling, and instance-lock release helpers.

- Added `tests/reference/test-grub-atomic-replacement.sh` with focused coverage for temporary same-directory generation, syntax validation, active-file preservation, permission retention, missing validators, symlink rejection, concurrent modification detection, failed atomic replacement, cleanup boundaries, source audits, and provisional JSON reporting.
- Added reusable GRUB transaction helpers for active-configuration fingerprinting, owner-only temporary-file preparation, `grub-script-check` validation, guarded cleanup, and same-directory atomic installation.

- Added `tests/reference/test-grub-blocked-after-initrd-failure.sh` with focused coverage for initrd validation failures, failed regeneration, missing initrd applicability, GRUB-only work, command-attempt boundaries, event suppression, provisional JSON, result diagnostics, and successful continuation after a valid initrd.
- Added a reusable boot-preparation coordinator and an independent GRUB prerequisite guard that require every scheduled initrd action to complete successfully before `grub-mkconfig` can be attempted.
- Added `tests/reference/test-initrd-installed-kernel.sh` with focused coverage for safe `mkinitrd.conf` parsing, post-update kernel-package resolution, stale and missing `KERNEL_VERSION`, installed module trees, output-path selection, apply-time command guards, schema defaults, source audits, and provisional JSON reporting.
- Added reusable initrd validation helpers that resolve the installed kernel from the validated post-update package snapshot, parse scalar `mkinitrd.conf` assignments without evaluation, verify the matching modules directory, and expose stable diagnostics.
- Added `tests/reference/test-elf-architecture-resolution.sh` with focused coverage for ELF32, ELF64, x86, x86-64, AArch64, endianness, exact soname matching, mixed-architecture loader caches, initial scans, post-rebuild verification, source audits, and provisional JSON metadata.
- Added normalized static ELF identity extraction and compatibility helpers that compare class, data encoding, and machine without executing either inspected objects or cached libraries.
- Added `tests/reference/test-elf-static-inspection.sh` with guarded executable fixtures, exact `DT_NEEDED` matching, symlink handling, parser-failure behavior, post-rebuild verification, fail-closed cache refresh, source audits, and provisional JSON assertions that prove inspected objects are never invoked.
- Added reusable static ELF helpers for magic-byte filtering, canonical regular-file resolution, `readelf -d` dependency extraction, `readelf -h` identity extraction, one-pass `ldconfig -p` cache normalization, exact architecture-compatible soname lookup, diagnostics, and post-rebuild reinspection.
- Added `tests/reference/test-sbo-personal-queue-protection.sh` with focused coverage for queue-directory resolution, private regular-file copies, nested queues, ignored symlinks, canonical path separation, successful and failed `sqg` isolation, apply-time source selection, cleanup boundaries, and provisional JSON reporting.
- Added reusable helpers that resolve direct, official default-value, and local sbopkg queue-directory assignments; prepare an owner-only per-run queue workspace without following personal queue symlinks; and generate private system/local `sqg` configuration wrappers.
- Added `tests/reference/test-sbo-options.sh` with focused coverage for safe option parsing, quoted values, queue option collection, persistent overrides, duplicate and conflict rejection, atomic failure behavior, ABI/ELF target option application, configuration defaults, provisional output, and final `sbopkg -b -B` submission.
- Added schema-1 `sbo.options_file` configuration with the backward-compatible default `/etc/slack-update/sbo-options.sqf` and normalized per-package SBo option records.
- Added reusable SBo build-option parsing, normalization, collection, rendering, and structured-output helpers.
- Added `tests/reference/test-sbo-dependency-order.sh` with focused coverage for dependency constraints, shared dependencies, deterministic topological ordering, filesystem-order independence, cyclic and invalid queue rejection, atomic replacement, final queue merging, and `sbopkg -b -B` submission.
- Added reusable SBo queue-graph helpers that combine generated queue constraints and produce one deterministic dependency order.
- Added `tests/reference/test-sbo-target-selection.sh` with focused coverage for active queue records, build-option syntax, recursive references, deselected records, unsafe names, C-locale normalization, filesystem-order independence, exact installed SBo detection, broken-object ownership, atomic failure behavior, and final target submission.
- Added reusable SBo target-selection helpers for queue records, installed SBo packages, and deterministic target-set merging.
- Added `tests/reference/test-partial-slackware-update.sh` with focused coverage for failures in `slackpkg update`, `install-new`, and `upgrade-all`, post-failure snapshot capture, blocked module state, event suppression, provisional JSON reporting, and the successful continuation path.
- Added validated package snapshot capture with canonical normalization, deterministic ordering, duplicate rejection, record counts, and atomic replacement.
- Added `tests/reference/test-package-snapshots.sh` with focused coverage for valid, empty, missing, malformed, unsorted, duplicate, and non-canonical snapshots, plus apply-workflow guards.
- Added a reusable Slackware package-record parser that extracts name, version, architecture, and build from the rightmost fields used by `pkgtools`.
- Added `tests/reference/test-package-name-parsing.sh` and representative Slackware 15.0, Slackware-current, patched-package, multi-hyphen, plus-sign, and SBo fixture records.
- Added stable process exit codes `0` through `8`, including dedicated statuses for partial updates, unsafe boot preparation, successful reboot guidance, concurrent execution, invalid input, and unavailable privilege.
- Added stable exit-code descriptions to human-readable summaries and final JSON results.
- Added `enabled`, `disabled`, and `auto` activation modes for the optional Flatpak, SBo, ELF, Cinnamon, and boot modules.
- Added explicit requirement and applicability probes for every optional module, with activation state and reason reporting in human-readable, JSON, and event output.
- Added schema-1 compatibility defaults so configurations created before module modes were introduced continue to load with omitted modes interpreted as `auto`.
- Added `data/config/slack-update.conf` as the schema-versioned system configuration for the shell reference.
- Added a strict, non-evaluating INI-style parser that rejects unknown sections and keys, duplicates, missing values, invalid booleans, unsafe paths and tokens, and unsupported schema versions.
- Added Slackware 15.0 and Slackware-current as explicit, mandatory compatibility targets in the roadmap and contribution policy.
- Added `--check` as a non-destructive Slackware repository update check using `slackpkg check-updates`.
- Added `--apply` as an explicit selector for the existing update workflow.
- Added `--dry-run` to inspect the current host state and print the complete apply sequence, conditional triggers, current SBo queues, ABI rebuild candidates, ELF findings, optional-component availability, and boot preparation actions without executing update, synchronization, build, installation, initrd, or bootloader commands.
- Added `--json` for one provisional structured result document on standard output.
- Added `--events` for provisional newline-delimited JSON progress events covering operation, module, action, warning, error, and completion states.
- Added a C-oriented `.gitignore` covering prospective Meson and CMake build output, compiler artifacts, test output, and common editor files.
- Added `CONTRIBUTING.md` with the English-language policy, phase-gate rules, reference-script validation requirements, and commit-message convention.
- Added the GNU General Public License version 3, with the project designated as `GPL-3.0-or-later` in the README.
- Added this changelog using the Keep a Changelog structure.

### Fixed

- The GRUB atomic-replacement harness now isolates `PATH` while simulating an unavailable `grub-script-check`, so the fail-closed branch remains deterministic even when the host running the tests has a real validator binary installed.
- Slackware-current kernel boot discovery no longer requires an initrd unconditionally. A coherent direct GRUB boot is accepted only when `mkinitrd.conf` and `initrd.gz` are both absent, `/boot/vmlinuz-generic` resolves to the package-owned `vmlinuz-VERSION`, repository metadata contains the target versioned image, and the running `BOOT_IMAGE` is present in a syntax-valid GRUB configuration. Mixed states still fail closed.
- Package-database enumeration now follows only the command-line compatibility symlink with `find -H`, so Slackware 15.0 and Slackware-current installations whose `/var/log/packages` points to the real `pkgtools` database produce valid snapshots, installed-SBo target sets, and exact package-name lookups without following symlinks stored inside the database.
- No-updates host metadata now records the configured and resolved package-database paths and follows the compatibility symlink when listing installed `slackpkg` records.
- The no-updates acceptance scenario now invokes the readable reference script through `bash` after syntax validation instead of requiring its executable bit to survive ZIP extraction or shared-folder copying.
- Default evidence parent directories are published with traversable permissions, while the `.tar.gz` archive and SHA-256 sidecar are assigned to the original `sudo` caller with mode `0600`; the expanded evidence directory remains root-only.
- Failed structured check or apply validation now prints the stable exit code, each Slackware command status, and the structured error list before archiving evidence.

### Changed

- Normal-update apply validation now distinguishes the broad `kernel_changes` package-group indicator from explicit boot requirements. Changes to `kernel-firmware`, `kernel-source`, or headers may report `kernel_changes=true` without requiring stable code `5`; only `initrd_required` or `grub_required` mandates successful boot preparation and reboot-required status, while critical userspace updates without boot work correctly use stable code `4`.
- Unattended `slackpkg install-new` and `upgrade-all` now use `-postinst=off`; the reference preserves active configuration files, never supplies the generic batch answer `y` to the `K/O/R/P` new-configuration menu, inventories pending regular `/etc/*.new` files after the package transaction, and exposes the deferred policy, scan validity, count, paths, and warnings in human and JSON results.
- Normal-update and no-updates acceptance validators now require the explicit deferred post-install policy and validate pending `.new` metadata without treating a non-empty list as a package-transaction failure.
- The real-system acceptance documentation now records accepted no-updates results for Slackware 15.0 and Slackware-current, points to both sanitized evidence fixtures, and documents a single-line `install` fallback that avoids invisible paragraph separators when copying evidence into the `promano` acceptance account.
- The reference now normalizes its root execution environment before argument parsing: `PATH` includes Slackware administrative directories, identity and temporary-directory variables use deterministic root values, locale and terminal behavior are non-interactive, desktop-session variables are removed, Git/SSH credential prompts are disabled, and `umask 077` protects newly created runtime state.
- The documented cron example now uses explicit `--check` rather than the backward-compatible implicit apply operation.

- Runtime initialization now fails closed when start-time capture, runtime-directory creation, temporary-file creation, dry-run workspace creation, or dry-run state-file creation fails; the selected operation is not started and the process returns stable code `1`.
- Logging setup now verifies required commands and writability before redirecting output, and completion-time capture failure is folded into the final non-zero result instead of being silently ignored.

- Runtime traps are now installed immediately after successful lock acquisition, before runtime initialization creates directories or temporary files.
- `SIGHUP`, `SIGINT`, and `SIGTERM` now disable recursive traps, run ownership-aware cleanup, release and close the instance lock, and terminate with statuses `129`, `130`, and `143` instead of returning to the interrupted workflow.
- Failed lock acquisition now closes file descriptor 9 before returning the stable concurrent-execution status.

- `grub-mkconfig` now writes only to an unpredictable owner-only temporary file beside the active configuration; the active `grub.cfg` path is never passed to the generator.
- Generated GRUB configuration must be a readable, non-empty regular file and pass `grub-script-check` before replacement; generator, validator, and install failures preserve the active file.
- GRUB replacement now rejects active symlinks and configurations outside the configured GRUB directory, detects concurrent active-file changes by fingerprint, preserves existing ownership and permissions, and uses a same-directory `mv -T` for atomic installation.
- Boot probing now requires both `grub-mkconfig` and `grub-script-check`, dry-run documents the staged transaction, cleanup removes only owned regular temporary files, and provisional JSON exposes generation, validation, installation, and replacement state.

- GRUB configuration generation is now blocked before its action-start event when required initrd validation or regeneration fails; `update_grub_configuration` repeats the same guard defensively at the command boundary.
- Required but unavailable initrd preparation in auto mode now blocks a simultaneously scheduled GRUB update instead of permitting bootloader generation without a validated initrd.
- Provisional JSON and human-readable diagnostics now distinguish a deliberate `grub_state=blocked` safety outcome from an attempted `grub-mkconfig` failure and report whether the GRUB command was ever invoked.
- Initrd regeneration now derives the target version from the installed `kernel-generic` package in the validated post-update snapshot, requires an exact `KERNEL_VERSION` match, verifies `/lib/modules/<version>`, and refuses to invoke `mkinitrd` when any check fails.
- Boot probing now treats an installed `mkinitrd` command plus a regular configuration file as an applicable validation path, so malformed or stale configuration fails closed during a triggered update instead of being silently skipped in auto mode.
- Initrd output validation now recognizes `OUTPUT_IMAGE`, retains compatibility with `OUTPUT`, and otherwise uses the configured default path; apply and dry-run JSON identify the package snapshot as the kernel-version source.
- Static loader-cache records now retain the resolved library path together with its ELF class, data encoding, and machine; a `DT_NEEDED` entry is satisfied only by an exact soname record whose three identity fields match the inspected object.
- Initial broken-object detection and post-`sbopkg` verification now use the same architecture-aware compatibility check, so an ELF32, x86-64, AArch64, or opposite-endian library cannot satisfy an incompatible object merely because the soname matches.
- Provisional apply and dry-run JSON now report `architecture_specific_resolution=true`, the exact `class`, `data`, and `machine` match fields, and the number of usable architecture-tagged cache records.
- Initial broken-object detection and post-`sbopkg` verification now share the same static `readelf` plus loader-cache path; the former dynamic-loader verification command has been removed, inspected objects are never executed, and unreadable objects remain unresolved during post-build verification.
- ELF scans now build an owner-only architecture-tagged library cache once per inspection, reject empty cache results, require exact soname and ELF-identity matches rather than substring or cross-architecture matches, skip non-ELF and non-regular targets before `readelf`, and fail the module when static cache preparation fails.
- Provisional apply and dry-run JSON now report `inspection_method=readelf+ldconfig-cache`, `executes_inspected_objects=false`, and the static scan and verification statuses.
- `sqg -a` now receives an owner-only `SBOPKG_CONF` wrapper that loads the original system and local configurations and then reasserts Slack-Update's disposable `QUEUEDIR` after both layers; the configured personal queue directory is never passed to the generator or modified by queue regeneration.
- Failed workspace preparation or a non-zero `sqg` result now blocks SBo target construction and final queue submission instead of falling back to personal queues or consuming a partially generated workspace.
- Apply-time SBo target, dependency, and build-option collection now reads the private queue copy, while dry-run continues to inspect the configured personal queues without modifying them.
- Private queue workspace preparation now fails closed for relative paths, symlinked queue roots, pre-existing destinations, or destinations equal to or canonically inside personal queue state; regular `.sqf` files are copied with modes `0600` under a `0700` workspace and queue symlinks are ignored.
- Cleanup now removes only the generated queue workspace whose owned path and canonical identity were recorded at creation; rejected pre-existing paths and later symlink replacements are left untouched.
- Provisional SBo JSON now distinguishes the personal queue directory from the generated workspace and reports isolation, copied-file, and ignored-symlink metadata.
- Final SBo queues now preserve safe per-package `NAME=value` build options while retaining dependency order and deterministic ABI/ELF target appending.
- Options present in current generated queues are deduplicated and checked for conflicts, while the optional persistent `sbo.options_file` takes precedence so custom choices survive `sqg -a` regeneration.
- Invalid, unsafe, duplicate, or contradictory SBo option records now fail atomically, preserve previous queue state, block `sbopkg`, and appear in dry-run and provisional JSON reporting.
- Generated SBo queue records are now combined as dependency constraints and resolved with a deterministic topological order instead of being alphabetically reordered.
- Shared SBo dependencies now appear once, independent packages use a C-locale tie break, and cyclic or contradictory queue constraints fail atomically before `sbopkg` runs.
- Final SBo queue construction now preserves the dependency-ordered core and appends unique ABI and broken-ELF targets in deterministic order.
- SBo target selection now ignores comments, recursive queue references, and deselected records, extracts package names independently from build options, rejects unsafe names, and produces a C-locale sorted unique target set independent of queue-file enumeration order.
- ABI rebuild candidates and broken-object SBo owners now share the same canonical target validation and deterministic target-set merge rules.
- Invalid SBo target selection now fails the SBo module, is exposed in provisional JSON, and blocks queue submission to `sbopkg`.
- Apply now stops after the validated post-update snapshot when any Slackware package operation fails, preventing Flatpak, package-change analysis, SBo, ELF, Cinnamon, initrd, and GRUB work from running against a partial Slackware state.
- Partial Slackware updates now mark every secondary module as `blocked`, include a stable blocking reason in summaries and provisional JSON, and emit no secondary-module start events.
- Package snapshots now fail closed when the package database cannot be enumerated or validated instead of silently creating incomplete state with `|| true`.
- Apply now stops before invoking `slackpkg` when the baseline snapshot is invalid and stops snapshot-dependent follow-up work when the post-update snapshot is invalid.
- Apply summaries and provisional JSON results now report snapshot validity, package counts, and validation errors for the before and after states.
- Replaced prefix-based package snapshot matching with literal parsed package-name comparison, preventing collisions such as `openssl` versus `openssl-solibs`.
- Replaced `rev | cut` SBo-name extraction and arbitrary tag filtering with parsed build-suffix checks for dry-run candidates, ABI rebuild targets, and broken-object ownership.
- Cinnamon package detection now compares the parsed package name exactly instead of relying on a filename glob.
- The reference script can now be sourced by regression tests without invoking `main()`; direct execution remains unchanged.
- Final JSON results now set `exit_code_stable` to `true`, include `exit_code_meaning`, and preserve `success=true` for successful reboot-recommended (`4`) and reboot-required (`5`) outcomes.
- The final `operation_completed` NDJSON event now carries the stable process exit code; intermediate action events continue to expose raw external-command statuses.
- Invalid arguments and configuration now return `7`, missing privilege returns `8`, and a concurrent instance returns `6` before workflow execution begins.
- Apply-result precedence is now unsafe boot (`3`), partial or verification failure (`2`), reboot required (`5`), reboot recommended (`4`), then success without reboot (`0`).
- Kernel headers changes alone no longer produce a reboot-required result; code `5` is tied to kernel image or module changes that schedule boot preparation.
- Optional modules now execute only when permitted by their configured activation mode.
- `enabled` reports missing module requirements as errors, `disabled` bypasses probing and execution, and `auto` treats unavailable or irrelevant optional software as non-fatal.
- Cinnamon graphical ABI triggers are ignored when the Cinnamon module is disabled or not applicable.
- Boot auto mode independently selects validated initrd and GRUB preparation paths after kernel changes; disabled mode suppresses both actions, while enabled mode retains strict failure reporting.
- Provisional JSON results now include each optional module's configured mode, activation state, reason, and operation-specific final state.
- Moved runtime paths, log retention, package groups, SBo and boot paths, Cinnamon repository settings, ELF scan roots, and the Slackware `install-new` and `upgrade-all` decisions out of the script and into the configuration file.
- Kept executable command names and argument structures fixed in the reference script so configuration values cannot introduce arbitrary shell execution.
- Added the effective configuration path to human-readable summaries and provisional final JSON results.
- Renamed the reference script installation target, lock file, work directory, log directory, and runtime identity from `sbo-auto` to `slack-update`, without changing the update workflow.
- Split the linear reference workflow into 24 clearly named functions, including the `main()` coordinator, while preserving command order and observable behavior.
- Added two command-line interface functions for `-h` and `--help`, explicit rejection of unknown options and unexpected positional arguments, and no operational mode changes; the script then contained 26 named functions.
- Added mutually exclusive operation parsing for `--check`, `--apply`, and `--dry-run`; running without an operation remains a compatibility alias for apply.
- Added an isolated temporary workspace for dry-run queue and ELF inspection data so planning does not overwrite the persistent reference state under `/var/lib/slack-update`; normal locking and logging remain enabled.
- Reserved standard output for machine-readable data when either `--json` or `--events` is selected, while human-readable progress continues through standard error and the normal log.
- Made `--json` and `--events` mutually exclusive so each output stream remains unambiguous and directly parseable.
- Instrumented the existing check, dry-run, and apply coordinators with provisional operation, module, and action events without changing their human-readable execution order.
- Relocated the existing shell implementation to `tools/reference/slack-update-reference.sh` without changing its contents.
- Updated the roadmap to reflect the repository work completed in Phase 0.

### Development context

- Current roadmap phase: **Phase 1 — Stabilize and validate the shell reference**.
- Current continuation boundary: the step-81 reboot review is accepted with archive SHA-256 `e41138a31225a795c65ddf76fa75fde19072f5892c9719af32d06b564027937b`. The machine still runs 6.18.40, the exact generic kernel/initrd pair targets 6.18.42, `pause_safe=true`, and one manual reboot is authorized but not yet executed. Do not refresh Slackpkg metadata or repeat any candidate, payload, readiness, apply, recovery, or reboot-review stage. Reboot once, then run the separate `current-kernel-post-reboot-verification` boundary before closing the update.
- Phase 0 is complete, committed as `3064cfa`, tagged as `planning-v1`, and published to GitHub.
- The first twenty-six Phase 1 refactoring, interface, parsing, and safety tasks are complete: runtime identifiers use `slack-update`, the script is split into named functions, argument parsing is available, `--check`, `--apply`, and `--dry-run` are implemented, `--json` emits a final structured result, `--events` streams machine-readable progress, operational settings are loaded from a validated configuration file, optional modules implement `enabled`, `disabled`, and `auto`, process exit codes `0` through `8` are stable, every represented error outcome is verified to produce a non-zero process result, and detached cron-style execution is validated under a normalized minimal environment.
- Phase 1 real-system acceptance began with a reproducible no-updates scenario, expected structured fixtures, evidence capture, and automated harness validation. Both mandatory targets passed on 2026-07-28. Slackware 15.0 preserved 1,594 package records with reviewed archive SHA-256 `5a784cd6d830ac271cc3aad02ed89f2e00c2afd63c88f90aabb74b0a81b0b20b`; Slackware-current preserved 2,035 package records with reviewed archive SHA-256 `ba0c1264d57df5acf6bee843391113327736683ede36ec3d708d58ca174a2976`. Both runs returned stable code `0` for check and apply, preserved raw `slackpkg` no-package statuses `20`, left the observed initrd and GRUB state unchanged, and passed all six scenario assertions.
- The physical Slackware-current normal-update transaction was reviewed on 2026-07-29 with archive SHA-256 `00679a69d9c40033db74b4a73525651a42d0d72b293ec536d1181e87e2ab7e66`. It refreshed and matched the reviewed ten-package candidate digest, returned stable code `0`, upgraded exactly those ten packages, retained 2,039 installed-package records, detected an ABI change through PipeWire, and left kernel, initrd, and GRUB state unchanged. Its nine scenario assertions passed. The evidence also showed 27 `.new` files reaching slackpkg's interactive post-install menu with the global answer `y`; the package transaction is accepted, but the current `-postinst=off` plus explicit inventory policy requires a later real-update revalidation before the Slackware-current row is closed.
- Slackware 15.0 normal-update preflight and apply were reviewed on 2026-08-01. After three boot-kernel packages were temporarily deferred, preflight archive SHA-256 `4774070e7a9173f6486d560e54d7efce8580b76a92449e1698e7449d8557e73c` approved exact candidate-set SHA-256 `baaf89bb3e61662d7bbb10223e2c26b9adc98c443eacdf8273319a6818951410` with 12 new packages, 184 upgrades, five critical candidates, and no boot-kernel candidates. Apply archive SHA-256 `c670a5077f9efb5d64470b46b537754634913c0f1deccd4fcef707c9385339ed` returned stable code `4`, installed or changed all 196 candidates, moved the package database from 1,554 to 1,566 records, preserved initrd and GRUB state, and reported 45 deferred `.new` files without invoking Slackpkg's interactive post-install menu. The observed one-assertion failure was a validator false negative caused by conflating broad kernel-related changes (`kernel-firmware` and `kernel-source`) with boot-kernel replacement; the corrected contract accepts the evidence.
- The script currently contains 164 named functions.
- Running without an operation still selects apply for backward compatibility with the original reference behavior.
- `--json` and `--events` both retain provisional schema version `0`; their complete schemas are not stable APIs yet. The final process exit code is stable: `--json` marks it with `exit_code_stable=true`, and the final `operation_completed` event uses the same code. Intermediate event exit codes remain raw external-command statuses.
- Machine-readable modes reserve standard output. Human-readable progress and external-command output are written to standard error and the normal log.
- `--json` and `--events` are mutually exclusive. The current event types are `operation_started`, `module_started`, `action_started`, `action_completed`, `module_completed`, `warning`, `error`, and `operation_completed`.
- Dry-run uses `slackpkg check-updates` as its only Slackware command and otherwise performs local state inspection. It does not refresh package metadata, so exact changed package names and package-derived ABI or kernel triggers remain conditional until apply compares its before and after snapshots.
- Dry-run requires root in the current reference implementation because it shares the existing instance lock and system log. Its queue, package-candidate, and ELF scratch files are isolated under a temporary directory and removed at exit.
- Configuration schema version `1` is implemented for the shell reference. Optional-module activation modes and stable process exit codes are implemented.
- The system configuration path is `/etc/slack-update/slack-update.conf`; the source-tree fallback is `data/config/slack-update.conf`, and `SLACK_UPDATE_CONFIG` may select an absolute test fixture.
- Exact Slackware package-name parsing is validated with 71 focused checks.
- Package snapshots before and after updates are validated with 38 focused checks. Partial Slackware update handling is validated with 76 focused checks across each `slackpkg` operation, blocked module states, event suppression, provisional JSON, no-package status `20`, and the successful continuation path. Deterministic SBo target selection is validated with 51 focused checks, dependency-ordered queue construction is validated with 32 focused checks, SBo build-option preservation is validated with 57 focused checks, personal queue isolation is validated with 92 focused checks, static ELF non-execution is validated with 41 focused checks, architecture-specific ELF resolution is validated with 43 focused checks, installed-kernel initrd validation is validated with 69 focused checks, GRUB blocking after initrd failure is validated with 55 focused checks, staged GRUB validation plus atomic replacement is validated with 87 focused checks, interruption cleanup is validated with 57 focused checks, non-zero error propagation is validated with 137 focused checks, minimal cron execution is validated with 79 focused checks, the no-updates acceptance harness is validated with 65 focused checks, the normal-update acceptance harness is validated with 106 focused checks, the kernel boot-path preflight is validated with 33 focused checks, the ELILO generator and versioned-source preflight is validated with 65 focused checks, the ELILO kernel transaction preflight is validated with 72 focused checks, the staged ELILO kernel apply transaction and accepted reboot record are validated with 92 focused checks, ELILO oldkernel retention eligibility and evidence portability are validated with 109 focused checks, boot-loader-neutral kernel cleanup planning is validated with 97 focused checks, cleanup dry-run gating and recovery simulation are validated with 103 focused checks, Slackware-current kernel boot-layout discovery is validated with 82 focused checks, direct generic-kernel transition policy is validated with 57 focused checks, exact kernel-package transaction inspection is validated with 51 focused checks, installed `geninitrd` policy discovery is validated with 47 focused checks, DKMS-hook discovery is validated with 51 focused checks, command-output-only GenInitrd projection is validated with 57 focused checks, GenInitrd/GRUB ownership planning is validated with 53 focused checks, ownership implementation is validated with 58 focused checks, generated-initrd post-state validation is covered by 46 focused checks, candidate-chain refresh classification is validated with 62 focused checks, kernel-chain restart validation is covered by 50 focused checks, and deferred slackpkg post-install handling is validated with 23 focused checks. The next development tasks are to repeat the Slackware-current normal-update preflight against fresh metadata, rebind the accepted kernel evidence chain to that exact candidate set, repeat the Slackware 15.0 retention preflight after `2026-08-08T19:51:00+02:00`, and design a separately authorized cleanup apply only after mature eligibility is accepted.
- The thirty-seven focused reference suites total 2,412 checks. Step 54 expands boot-layout discovery to 82 checks after the step-53 diagnostic revoked the direct-no-initrd baseline. The hardened exact-package preflight contributes 51 checks, the accepted `geninitrd` policy preflight contributes 46 checks, the DKMS-hook discovery harness contributes 50 checks, the GenInitrd command-output harness contributes 57 checks, the GenInitrd/GRUB ownership preflight harness contributes 55 checks, the ownership engine harness contributes 58 checks, the generated-initrd post-state harness contributes 46 checks, the candidate-chain refresh harness contributes 62 checks, the current-kernel boot harness contributes 82 checks, and the kernel-chain restart harness contributes 50 checks. The main reference implementation now has SHA-256 `0dc4a4def9b063b9a598975f46e7458c5771eb8d8603f4fa8bbd9dfc07c4d4c6`; the static ELF suite remains byte-identical with SHA-256 `dbf40988eae32190d9faaa7703fd78edfdb5a6dbd99c115f405b15a40b8c49ba`.
- Package-name parsing tests cover path and archive normalization, right-to-left field extraction, malformed records, literal snapshot matching, prefix collisions, plus signs, multi-hyphen names, patched builds, and exact `_SBo` build suffixes.
- Package snapshot tests cover canonical normalization, deterministic C-locale ordering, command-line package-database symlinks, rejection of internal record symlinks, duplicate rejection, atomic replacement, missing and empty package databases, malformed and non-canonical records, stale snapshot removal, record counts, and workflow guards before and after Slackware operations.
- Partial-update tests confirm that a non-zero status from `update`, `install-new`, or `upgrade-all` still permits final snapshot capture but blocks every secondary module, suppresses their start events, reports `blocked` states in provisional JSON, and leaves the successful path unchanged.
- SBo target-selection tests cover queue options, comments, recursive references, deselected entries, unsafe names, duplicate removal, C-locale ordering, directory-layout independence, package-database compatibility symlinks, exact `_SBo` ownership, installed-package deduplication, deterministic target-set union, and final submission guards.
- SBo dependency-order tests cover generated precedence constraints, shared dependencies, deterministic topological ordering, non-alphabetical dependencies, cyclic and contradictory constraints, atomic failure behavior, ordered final merging, and the final `sbopkg -b -B` submission file.
- SBo build-option tests cover safe assignment grammar, quoted values, generated queue options, persistent override precedence, missing optional files, duplicate and conflict rejection, atomic replacement, options on ABI/ELF targets, configuration defaults, and exact final queue submission.
- SBo personal-queue protection tests cover quoted, fallback, official default-value, and local-override directory resolution; regular and nested queue copies; ignored queue symlinks; canonical path separation; owner-only workspace and configuration modes; owned cleanup and replacement-symlink refusal; global/local configuration reassertion; successful and failed `sqg` isolation; private apply-time queue selection; fail-closed workspace preparation; and provisional JSON fields.
- Static ELF inspection tests cover magic-byte filtering, regular-file and symlink resolution, normalized `DT_NEEDED` and identity extraction, architecture-tagged cache generation, guarded executable fixtures, parser failures, post-rebuild reinspection, cache-refresh failures, source-level prohibition of loader trace execution, and provisional JSON metadata.
- ELF architecture-resolution tests cover exact class, data-encoding, and machine matching across ELF32 x86, ELF64 x86-64, AArch64, and opposite-endian records; mixed caches; missing and malformed metadata; initial scans; post-rebuild verification; source audits; and provisional JSON fields.
- Installed-kernel initrd tests cover non-evaluating scalar parsing, exact post-update `kernel-generic` version resolution, stale running-kernel values, missing or ambiguous package records, required module trees, output-path compatibility, guarded `mkinitrd` invocation, schema defaults, source audits, and provisional JSON fields.
- GRUB-after-initrd safety tests cover validation failure, failed regeneration, missing initrd applicability, GRUB-only operation, direct command failure, coordinator events, command-attempt metadata, blocked result diagnostics, and both coordinator and command-boundary guards.
- GRUB transaction tests cover owner-only same-directory staging, non-empty and syntax validation, active symlink and outside-path rejection, byte-preserving failures, permission preservation, concurrent modification detection, atomic move failure, guarded cleanup, source audits, and provisional JSON fields.
- Signal-cleanup tests cover real `SIGHUP`, `SIGINT`, and `SIGTERM` delivery, statuses `129`, `130`, and `143`, lock observation and reacquisition, owned scratch files, runtime directories, staged GRUB files, private SBo workspaces, unrelated-file preservation, pre-runtime interruption, workflow non-resumption, and trap-order source guards.
- Error-exit tests cover all values `0` through `8`; check, dry-run, partial apply, initrd, GRUB, package-snapshot, Slackware, Flatpak, SBo, Cinnamon, ELF, and enabled-module failures; successful non-zero reboot outcomes; invalid input and configuration; privilege and lock failures; runtime and logging setup; completion-time capture; and final JSON plus NDJSON result propagation.
- Cron-environment tests cover direct and harnessed `env -i` execution, replacement of hostile inherited variables, `/usr/local/sbin`, `/usr/sbin`, and `/sbin` command visibility, detached standard streams, `/dev/null` input, non-interactive Slackware checks, human/JSON/NDJSON separation, prompt suppression, and owner-only work, log, and lock files.
- No-updates acceptance-harness tests cover mandatory target names and version matching, explicit permission for real apply operations, source-level check-before-apply ordering, optional-module isolation, schema-1 configuration generation and parsing, positive and negative structured-result validation, actionable failure details, compatibility-symlink metadata, deterministic package-database hashing, interpreter-based execution, evidence archive contents, caller ownership, private file modes, SHA-256 sidecar verification, the single-line copy fallback, and sanitized accepted records for Slackware 15.0 and Slackware-current.
- Module-mode tests cover all modules disabled, all requirements available in auto mode, invalid mode values, enabled modules with missing requirements, non-fatal auto unavailability, kernel changes with boot auto and boot enabled, and schema-1 configurations without explicit mode keys.
- Disabled modules were verified not to invoke Flatpak, SBo, ELF, Cinnamon, initrd, or GRUB commands in the deterministic mock environment.
- JSON results were parsed for apply and dry-run activation scenarios. NDJSON events were parsed, checked for contiguous sequence numbers and required first/final event types, and verified to expose disabled module states.
- Syntax validation passed with `bash -n`; the executable mode remains `0755`, no C source files were added, and no trailing whitespace was detected. ShellCheck was not installed in the validation environment.
- The reference script has passed the fully updated no-updates acceptance scenario on Slackware 15.0 and Slackware-current. The Slackware 15.0 non-boot-kernel normal-update branch is now accepted, including real-system validation of the hardened deferred `.new` policy. The reviewed Slackware-current transaction remains accepted as package and boot evidence, while target-specific `.new` revalidation on Slackware-current and the dedicated Slackware 15.0 boot-kernel scenario remain pending.

- Phase 1 step 109 is prepared after the real step-108 recovery-backup release review failed safely on 2026-08-15 with 7 passes, 2 failures, 1 skip, and archive SHA-256 `d1da16ad0635c1db89eae273c953fd005b65759699e94e4878676f34fa575003`. Manual review proves both failures were review-boundary defects rather than machine-state drift: step 108 emitted active-module paths without the `./` prefix used by the accepted step-107 manifest, while all 4,652 module object hashes and paths match exactly after that representation-only normalization; and the private recovery directory contains the ten files created by the step-105 recovery snapshot, not only the three payload tarballs. The failed review was non-mutating, the three payload archives remained hash-valid, and all captured before/after state remained identical. Step 109 records the failed evidence as a diagnostic and adds a read-only revision-1 release review that restores the step-107 module path representation and requires the exact ten-file recovery snapshot. The complete recovery-directory manifest SHA-256 is `eabe41e119b8a9233b0d19f4e6c57e3b6510bc59fb070ce3854a72fa99878d56`; the future ten-file removal-target SHA-256 is `a18c56ff099c424cea5e16b3ec559debd8e54d7fec4968c6c0154dd2216831a8`. Removal execution remains separately disabled.

- Phase 1 step 110 is prepared after the real step-109 revision-1 recovery-backup release review passed on 2026-08-15 with 12 passes, 0 failures, 0 skips, archive SHA-256 `7c18beea914406dfe0354169b683839c14a03479e3a29dc869d1c2119facb624`, and removal-target SHA-256 `a18c56ff099c424cea5e16b3ec559debd8e54d7fec4968c6c0154dd2216831a8`. The accepted review keeps the exact ten-file recovery snapshot retained while authorizing only a later separately gated removal. Step 110 adds that destructive boundary. It is bound to the accepted step-109 record, the exact step-109 review code and policy, the same verified 5.15.209 boot, the accepted package/module/ELILO/boot baselines, the complete ten-file recovery manifest, and the exact removal target. Before mutation it revalidates all state and every recovery file; immediately before each unlink it rechecks that member's exact SHA-256. It then permits exactly ten `unlink` operations followed by one `rmdir` of the exact now-empty backup directory. Recursive/glob/parent removal and all unrelated system mutation remain denied. A complete removal must preserve package, ELILO, boot, active-module, and rollback-absence state and yields `pause_safe=true`; incomplete or partial removal fails closed to manual review.

- Phase 1 step 111 is prepared after the real step-110 authorized recovery-backup removal failed closed on 2026-08-15 before any destructive action. The failed archive SHA-256 is `8cc762579a8e32f23c197d26bc260fcc375d8152645f56f69e5bbe2eff26c00f`; it records `removal_started=false`, `removed_file_count=0`, `removal_executed=false`, `recovery_backup_retained=true`, and byte-identical package/module/ELILO/boot state before and after the attempt. The persistent removal target also remained the exact authorized ten-file snapshot. The visible runtime mismatch is the transient Linux boot ID: step 109 bound `626f1a3a-606a-4dd3-8ff2-64d78032cadf`, while step 110 observed `a67cdf84-8ae2-4b3d-96e9-7c4cfc7bd8a3` after the previously declared safe pause. Requiring boot-ID equality across a pause that permits powering off the VM is therefore over-constrained. Step 111 adds a read-only boot-rebind review. It preserves the previous and observed boot IDs as diagnostic history but no longer treats equality to either UUID as a removal prerequisite. Instead it requires stable runtime identity: exact host, running kernel 5.15.209, a `BOOT_IMAGE` ending in `\\EFI\\Slackware\\vmlinuz-generic-5.15.209`, canonical package database availability, and the exact accepted package/module/rollback-absence/ELILO/boot/recovery baselines. A successful review only reauthorizes a later revision-1 destructive removal; step 111 itself cannot mutate or remove recovery data and establishes `pause_safe=true`.
